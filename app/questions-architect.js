/* ============================================================
   CCA Trainer · question data · Claude Certified ARCHITECT – Foundations
   ------------------------------------------------------------
   Domains, scenarios, the question bank and the exam descriptor for the
   Architect track (exam code CCAR-F). Loaded via a plain <script> tag
   (before cca-trainer.js) rather than fetch() — the app is opened as a
   local file, and fetch() of a local JSON file is blocked by the
   browser's file:// CORS policy.

   Everything here is namespaced ARCHITECT_* and gathered into the
   EXAM_ARCHITECT descriptor at the bottom of the file, because a second
   track (questions-associate.js) defines the same shapes for its own
   exam. cca-trainer.js never reads these names directly — it goes
   through the active track's descriptor.
   ============================================================ */

/* ---------- Domains (weights confirmed against the official Anthropic "Claude Certified
   Architect – Foundations" Exam Guide, v1.0, effective July 2026, exam code CCAR-F) ---------- */
const ARCHITECT_DOMAINS = [
  {
    id: "d1",
    name: "Agentic architecture & orchestration",
    short: "Agentic",
    weight: 27,
    color: "var(--d1)",
    hex: "#B0455D",
  },
  {
    id: "d2",
    name: "Claude Code · config & workflows",
    short: "Claude Code",
    weight: 20,
    color: "var(--d2)",
    hex: "#C98A2E",
  },
  {
    id: "d3",
    name: "Prompt engineering & structured output",
    short: "Prompt",
    weight: 20,
    color: "var(--d3)",
    hex: "#5E7F6E",
  },
  {
    id: "d4",
    name: "Tool design & MCP integration",
    short: "Tool/MCP",
    weight: 18,
    color: "var(--d4)",
    hex: "#4A6B82",
  },
  {
    id: "d5",
    name: "Context management & reliability",
    short: "Context",
    weight: 15,
    color: "var(--d5)",
    hex: "#7A5E8A",
  },
];

/* ---------- Scenarios (shared setup for a group of questions; mirrors the real
   exam's scenario-based format). A question links to one via its `sc` field. ---------- */
const ARCHITECT_SCENARIOS = {
  s1: {
    domains: ["d1", "d4", "d5"],
    title: "Customer Support Resolution Agent",
    context:
      "You're building a customer support resolution agent with the Claude Agent SDK. It handles high-ambiguity requests like returns, billing disputes, and account issues through custom MCP tools — get_customer, lookup_order, process_refund, and escalate_to_human. Your target is 80%+ first-contact resolution while knowing when to escalate.",
  },
  s2: {
    domains: ["d2", "d5"],
    title: "Code Generation with Claude Code",
    context:
      "You're using Claude Code to accelerate software development for code generation, refactoring, debugging, and documentation. You need to integrate it into your team's workflow with custom slash commands and CLAUDE.md configuration, and know when to use plan mode versus direct execution.",
  },
  s3: {
    domains: ["d1", "d4", "d5"],
    title: "Multi-Agent Research System",
    context:
      "You're building a multi-agent research system with the Claude Agent SDK. A coordinator agent delegates to specialized subagents — one searches the web, one analyzes documents, one synthesizes findings, and one generates reports — so the system can research topics and produce comprehensive, cited reports.",
  },
  s4: {
    domains: ["d4", "d2", "d1"],
    title: "Developer Productivity with Claude",
    context:
      "You're building developer productivity tools with the Claude Agent SDK. The agent helps engineers explore unfamiliar codebases, understand legacy systems, generate boilerplate code, and automate repetitive tasks, using the built-in tools (Read, Write, Bash, Grep, Glob) alongside Model Context Protocol (MCP) servers.",
  },
  s5: {
    domains: ["d2", "d3"],
    title: "Claude Code for Continuous Integration",
    context:
      "You're integrating Claude Code into your CI/CD pipeline to run automated code reviews, generate test cases, and give feedback on pull requests. You need to design prompts that produce actionable feedback while minimizing false positives.",
  },
  s6: {
    domains: ["d3", "d5"],
    title: "Structured Data Extraction",
    context:
      "You're building a structured data extraction system with Claude that pulls information from unstructured documents and validates the output against JSON schemas. It must maintain high accuracy, handle edge cases gracefully, and integrate with downstream systems.",
  },
};

/* ---------- Question bank (practice questions, not real exam items) ---------- */
const ARCHITECT_Q = [
  // D1 — Agentic (10)
  {
    d: "d1",
    src: "AR1-23",
    q: "Every subagent's result goes back to your coordinator, rather than to whichever agent needs it next. What does the hub buy you?",
    a: [
      "Lower latency, since the coordinator can start the next subagent the moment a result lands",
      "One place to see what happened, since the coordinator decides which findings travel on",
      "Shared context, since the coordinator can carry each result into the next subagent's prompt",
      "Fewer tokens, since the coordinator can pass on a trimmed version of each finding",
    ],
    c: 1,
    e: "Observability is the point. With every result passing through one agent there is a single place to see what each subagent produced, and a single place that decides which findings travel on — the guide's stated reasons for building the topology this way. What the routing does not do is put one subagent's findings into another's context; a subagent sees a finding when the coordinator sends it one. Nor does it save tokens or time, since a hub adds a hop rather than removing one. Those are the costs accepted in exchange for the control.",
  },
  {
    d: "d1",
    src: "AR1-67",
    q: "Your coordinator's prompt to each research subagent lists the exact searches to run, in order. Every report comes back thin wherever the topic did not fit that list, and the depth varies wildly from one report to the next. What should the prompt carry?",
    a: [
      "A worked example of a strong finished report, for the subagent to match",
      "The research goal and the quality criteria, leaving the route to the subagent",
      "The goal, plus the deadline and the maximum length of the report",
      "The goal, plus a per-area word budget so no part of the report comes back thin",
    ],
    c: 1,
    e: "A subagent handed a procedure returns the procedure, including where the procedure did not fit. Stating the goal and the standard a good answer has to meet is what lets each subagent adapt its route to what it actually finds, which is the reason for delegating to something that can reason at all. More steps only extends the script's reach; more subagents run the same script over more ground; and an exemplar constrains the shape of the answer rather than freeing the search.",
  },
  {
    d: "d1",
    q: "In an orchestrator/subagent setup you give each subagent its own isolated context. What's the main reason?",
    a: [
      "It reduces the number of API keys you need because each agent reuses the same session",
      "It keeps each agent's context focused and free of context pollution",
      "It lets the agents share memory automatically so they don't have to pass state between them",
      "It's a formal requirement in the MCP specification for all multi-agent systems",
    ],
    c: 1,
    e: "Focused context per agent = less noise, fewer tokens, more reliable answers. When everything shares one growing context, the relevant drowns in the irrelevant.",
  },
  {
    d: "d3",
    q: "You need to classify 100,000 independent documents. It's not urgent — the results can come in a few hours. What's most cost-effective?",
    a: [
      "Send them all as regular real-time calls at once, so the whole job finishes as fast as possible",
      "Use the Batch API — asynchronous processing at lower cost when you can tolerate some delay",
      "Build one big prompt with all the documents, so you avoid the per-call overhead",
      "Run a single autonomous agent that loops through the documents and classifies one at a time",
    ],
    c: 1,
    e: "Match the shape of the workload to the API mode. The Batch API trades latency for lower cost — perfect for high volume of independent tasks with no urgency. (Check the current discount in the live docs.)",
  },
  {
    d: "d5",
    src: "AR5-33, AR5-36",
    q: "Two research subagents fail. One source returns a 503 saying it is temporarily overloaded. The other returns a 403 because no credential for that archive was ever configured. How should the subagents divide the work of handling these?",
    a: [
      "Recover from the 503 locally, and send only the 403 up, since no local effort will produce a credential",
      "Both go straight to the coordinator, so every retry decision on the run is made in one place with a view of the whole budget",
      "Each keeps retrying its own failure until the run's time budget is gone, so the coordinator is only ever handed finished work",
      "Retry the 403 locally in case the credential appears, and send the 503 up for the coordinator to decide when to come back to it",
    ],
    c: 0,
    e: "The 503 says the source was busy, and a busy source is the kind of failure a second attempt settles — one the subagent can make without help. The 403 says no credential exists, and nothing the subagent does will produce one. Centralising every retry is defensible engineering and still wrong here, for a reason worth stating: the coordinator knows nothing about the 503 that the subagent does not already know, so the round trip costs a turn to learn what had already resolved itself. Retrying until the budget is gone fails the other way, spending the run on an outcome that cannot change. And handling the two the other way round inverts the only thing that matters: the status says which is which, and a retry is the remedy for a busy source rather than for an absent credential.",
  },
  {
    d: "d1",
    q: "When do you reach for an orchestrator-worker pattern instead of a single autonomous agent?",
    a: [
      "When the task is small and linear, so the orchestrator can drive the steps in a fixed order",
      "When the subtasks are heterogeneous and each benefits from a specialized worker or parallelism",
      "Always — orchestrator-worker is the most modern pattern and should be the default choice",
      "When you want to save tokens on simple tasks by splitting them into smaller calls",
    ],
    c: 1,
    e: "Orchestrator-worker pays off when you have distinct subtasks that each benefit from a specialized worker, or that can run in parallel. For small linear tasks it's overkill.",
  },
  {
    d: "d1",
    src: "AR1-141, AR1-142",
    q: "A three-week-old analysis session mapped a service that has since been through a dependency upgrade and a directory restructure — almost every file the agent read has moved or changed. Its architectural conclusions are still what you want. What is the more reliable move?",
    a: [
      "Resume it and run /compact first, so the stale transcript becomes a structured summary",
      "Resume it, since the transcript holds reasoning that a written summary would flatten",
      "Start a fresh session and pass in a written summary of the conclusions that still hold",
      "Start fresh and re-read the moved files before reusing any of the session's conclusions",
    ],
    c: 2,
    e: "Once most of the tool results are stale, the transcript is mostly wrong material that the model will still read as evidence, and the cost of that outweighs what the session's history is worth. Capture what survived — the conclusions, the decisions, the diffs that matter — as application state, and hand that to a new session. /compact does produce a structured summary, but it produces it in the same session and out of the same stale transcript, so it compresses the problem rather than leaving it behind.",
  },
  {
    d: "d1",
    q: "An agent occasionally produces an invalid action (wrong arguments). Which safeguard is most robust?",
    a: [
      "Validate tool input and output against a schema, and request a retry on a validation error",
      "Trust the raw model output, since a current model rarely produces invalid arguments in practice",
      "Log the error for the record and let the agent continue without correcting it",
      "Switch to a larger model, which is less prone to producing invalid actions",
    ],
    c: 0,
    e: "Guardrails: validate against a schema and re-prompt on error. You treat the model's output as something to be verified, not something you can trust blindly.",
  },
  {
    d: "d1",
    src: "AR1-63",
    q: "To keep the prompt short, your coordinator sends the synthesis subagent a three-line digest of what the search and analysis subagents found. The synthesis comes back reading confidently and having lost almost every specific. What should the coordinator have sent?",
    a: [
      "The complete findings from both subagents, written into the synthesis prompt itself",
      "The complete findings, written to a file that the synthesis subagent is told to open first",
      "A digest per subagent, so the synthesis can see which finding came from where",
      "The findings as a ranked shortlist, with the weakest-supported ones dropped",
    ],
    c: 0,
    e: "A synthesis subagent's input is the specifics themselves — the findings the search and analysis subagents actually produced — so a digest of them is the wrong artefact to hand over, however well written. The guide's instruction is to include the complete prior findings directly in the subagent's prompt, not a description of them. A digest per subagent preserves each one's wording and still hands over a digest; a ranked shortlist sends real findings but drops exactly the long tail a synthesis is there to weigh. A file the subagent is told to open is the near miss: the findings are complete, but whether they arrive depends on the subagent choosing to read it, while the prompt arrives with the spawn itself.",
  },
  {
    d: "d1",
    q: "Parallelizing independent subtasks across multiple agents primarily improves what?",
    a: [
      "The accuracy of each individual answer, because the agents can check each other's work",
      "Latency and throughput for work that is genuinely independent across the subtasks",
      "The model's underlying knowledge, since more agents cover more subject areas",
      "The security of the system, because the work is spread across several isolated processes",
    ],
    c: 1,
    e: "Parallelism gives you speed/throughput when the tasks don't depend on each other. It doesn't make each individual answer more correct — it just gets you many answers faster.",
  },

  // D2 — Claude Code (7)
  {
    d: "d2",
    src: "AR2-01",
    q: "What is the main purpose of a CLAUDE.md file in a project?",
    a: [
      "To store API keys, tokens and other secrets the project needs at startup",
      "To give Claude Code persistent project context that's loaded automatically at startup",
      "To replace the project's README so end users have a single file to deal with",
      "To define the CI/CD pipeline that runs automatically when code is pushed to the main branch",
    ],
    c: 1,
    e: "CLAUDE.md is the persistent context layer: how a codebase fits together, which conventions apply, which build and test steps are expected. It is read at the start of every session, which is also why secrets never belong in it.",
  },
  {
    d: "d2",
    src: "AR2-03, AR2-04",
    q: "A team of 20 works on a monorepo. Where should shared conventions live versus personal preferences?",
    a: [
      "Everything in each developer's personal config, so no one overrides anyone else's setup",
      "Shared conventions in a project-scoped CLAUDE.md committed to the repo, personal ones in user scope",
      "Everything in one global file on a shared server that the whole team points at",
      "Conventions don't need to be written down — experienced developers figure out the pattern on their own",
    ],
    c: 1,
    e: "Shared and committed = the whole team gets the same context. User-scoped config (e.g. ~/.claude) keeps personal preferences private without forcing them on others.",
  },
  {
    d: "d1",
    q: "What are hooks in Claude Code for?",
    a: [
      "To help you write better prompts by suggesting improvements as you work",
      "To run deterministic commands automatically on specific lifecycle events",
      "To connect Claude Code to the internet so it can fetch updated packages itself",
      "To save the conversation history between sessions so context survives a restart",
    ],
    c: 1,
    e: "Hooks are deterministic automation/guardrails tied to events. They're code that's guaranteed to run — not an instruction the model can choose to ignore.",
  },
  {
    d: "d1",
    q: "You want to guarantee that no one edits a sensitive folder. Best mechanism?",
    a: [
      "Write clearly in CLAUDE.md that the folder must not be touched under any circumstances",
      "A hook that catches the action and deterministically denies it before it can happen",
      "Explicitly ask Claude to leave the folder alone at the start of each session",
      "Delete the folder entirely and restore it manually when you actually need it",
    ],
    c: 1,
    e: "A prompt instruction is non-deterministic — it can slip. A hook that blocks the action gives a guarantee, and is the right tool when the requirement is absolute.",
  },
  {
    d: "d4",
    src: "AR4-64, AR4-65",
    q: "Your team's .mcp.json ships a shared MCP server, and locally every developer gets an approval prompt the first time a session uses it. The same repo now runs Claude Code headless in CI with claude -p. What happens to that server in the CI run?",
    a: [
      "It loads with no approval step at all, since a headless session has nobody to ask",
      "The run fails on startup with an unapproved-server error until someone clears the CI user's project choices",
      "It loads only if a teammate's earlier approval was committed along with the config file itself",
      "It is skipped entirely, since project scope applies to interactive sessions only and headless runs see just the user file",
    ],
    c: 0,
    e: "Claude Code asks before it uses a project-scoped server, but that question only exists where somebody can answer it: in claude -p runs, SDK sessions and cloud sessions the .mcp.json entries load as configured. The review therefore belongs to the moment the server is added to the repo, not to the session that picks it up.",
  },
  {
    d: "d2",
    src: "AR2-44",
    q: "How are skills best described, conceptually?",
    a: [
      "Third-party language models you download and run alongside Claude locally",
      "Packages of knowledge that Claude loads in when they're relevant",
      "A paid subscription that unlocks extra features in Claude Code",
      "A type of hook that runs automatically before every single tool call",
    ],
    c: 1,
    e: "Skills are on-demand 'folders' of procedure and context that load when the task matches. They expand what Claude *knows how* to do, without filling the context all the time.",
  },
  {
    d: "d1",
    q: "Compliance requirement: every AI change to the codebase must be logged and auditable. Best approach?",
    a: [
      "Trust that the model remembers to log every change it makes along the way",
      "Hooks for deterministic logging and review gates, combined with limited (scoped) access",
      "Turn off the AI tools entirely, since compliance and automation can't be reconciled",
      "Ask the developers to manually write down what was changed after each session",
    ],
    c: 1,
    e: "When the requirement is auditability, you can't rely on the model remembering. Hooks give guaranteed logging/gating, and scoped access limits what can happen at all.",
  },

  // D3 — Prompt engineering (7)
  {
    d: "d3",
    q: "What's the most reliable way to get machine-readable, structured output?",
    a: [
      "Ask the model in prose to respond in JSON, and parse what it returns as text",
      "Use tool use / structured output with an input_schema that forces the right shape",
      "Ask for the answer in Markdown and extract the fields you need from the formatting",
      "Set temperature to 0 and trust that the output is then valid JSON every time",
    ],
    c: 1,
    e: "A schema (via tool use / structured output) gives the model a shape to fill in, and you get a parseable result. Asking for JSON in free text is far more fragile at scale.",
  },
  {
    d: "d3",
    q: "What is prefilling the assistant turn used for?",
    a: [
      "To make the answer longer by giving the model an intro it can build on",
      "To steer the start of the answer — for example forcing JSON by prefilling an opening brace",
      "To hide the system prompt from the model so it can't reveal its instructions",
      "To cache the answer so identical questions later are answered without a new call",
    ],
    c: 1,
    e: "By seeding the start of the assistant's reply you steer the format from the first token. A classic trick: prefill '{' to lock the model into a JSON answer.",
  },
  {
    d: "d3",
    q: "What are XML tags in the prompt primarily good for?",
    a: [
      "Making the prompt nicer to look at without affecting how the model interprets it",
      "Delimiting the input so the model separates instructions, context and data",
      "Saving tokens, because tags compress the text the model has to read through",
      "Enabling tool use, since the model recognizes tags as tool calls",
    ],
    c: 1,
    e: "Clear delimiters (like <context>…</context>) help the model know what's what. It reduces the chance it mixes up your instruction with the data you pasted in.",
  },
  {
    d: "d3",
    q: "Where do durable role and behavior instructions belong?",
    a: [
      "In every single user message, so the model is reminded of the rules each time",
      "In the system prompt; the user message carries the concrete task",
      "In a dedicated tool the model calls to fetch the rules when it needs them",
      "In CLAUDE.md, which applies regardless of what context the call runs in",
    ],
    c: 1,
    e: "The system prompt sets the stable frame (who the model is and which rules apply). The user message is the specific task or input for this particular call.",
  },
  {
    d: "d3",
    src: "AR3-11, AR3-12",
    q: "Your code-review agent tags every finding Important or Nit. Run it twice over the same diff and the same issue comes back Important once and Nit the other time. What stabilises the labelling?",
    a: [
      "Rank each run's findings against one another and call the top few Important",
      "Ask the model to state its severity reasoning in each comment, so the label can be checked",
      "Define what each level means for this repo, with example findings at each one",
      "Have a second pass re-read the findings and correct any severity it disagrees with",
    ],
    c: 2,
    e: "A severity level is only reproducible if the level itself is defined somewhere the model can read, with worked cases showing where the boundary between the two falls. Anthropic's own code-review guidance says to redefine what Important means for your repo and state which classes of finding are Important and which are Nit at most. Ranking within a run makes the label relative to whatever else that run happened to find, so the same issue moves as its company changes. Stating the reasoning makes a label auditable without making it stable, and a second pass applies the same undefined judgment one step later.",
  },
  {
    d: "d3",
    q: "You're answering questions based on attached documents and want to reduce hallucination. Best move?",
    a: [
      "Tell the model to be more confident so it stops hedging, since hedging is what produces vague guesses",
      "Instruct it to answer only from the given context, cite the source, and say 'I don't know' when it's missing",
      "Increase temperature for more variation, so it finds several possible answers to choose from",
      "Remove the system prompt so the model isn't unnecessarily constrained by your instructions",
    ],
    c: 1,
    e: "Grounding: tie the answer to the given context, require citation, and give the model an explicit way out ('I don't know') when the answer isn't there. Then it stops making things up.",
  },
  {
    d: "d3",
    q: "When is chain-of-thought / 'thinking' most useful?",
    a: [
      "On simple lookups that need one short factual answer with no intermediate work",
      "On multi-step reasoning tasks where the model needs to think before answering",
      "When you want to make the answer shorter by letting the model summarize along the way",
      "Never — explicit reasoning just wastes tokens without making the answer better",
    ],
    c: 1,
    e: "Explicit reasoning helps on multi-step tasks. You can also separate the thinking from the final, structured answer so the user sees only the conclusion.",
  },

  // D4 — Tool/MCP (7)
  {
    d: "d4",
    q: "What characterizes a good tool description?",
    a: [
      "As short as possible, ideally just the name, to save space in the context",
      "Clear on what the tool does AND when to use it, with well-typed, described parameters",
      "A thorough technical description of how the tool is implemented under the hood",
      "No description is needed as long as the tool's name is descriptive enough",
    ],
    c: 1,
    e: "The model picks tools based on the description. 'What it does + when to use it + clear parameters' produces the right choice and the right arguments. Vague descriptions produce wrong calls.",
  },
  {
    d: "d4",
    q: "What is MCP, in one sentence?",
    a: [
      "A dedicated Claude model specialized for coding and tool use in the terminal",
      "An open protocol that standardizes how apps give models context and tools",
      "A billing system that meters and invoices API usage across tools",
      "A frontend framework for building user interfaces around language models",
    ],
    c: 1,
    e: "The Model Context Protocol is the standardized 'plug' (often compared to USB-C for AI) between a host app and the tools/data sources it exposes.",
  },
  {
    d: "d4",
    q: "What is the role of a tool's input schema?",
    a: [
      "To document the tool for the humans who'll read the code later",
      "To constrain and validate the arguments (JSON schema) so the model produces valid calls",
      "To decide which model should handle the call when the tool is used",
      "To cache the result of the tool call so identical calls don't have to run again",
    ],
    c: 1,
    e: "The schema is the contract for the arguments. It reduces invalid actions by making it clear — and machine-verifiable — what a valid call looks like.",
  },
  {
    d: "d4",
    q: "An MCP server exposes powerful actions (delete, transfer money). Best practice?",
    a: [
      "Expose every action openly, since restricting the tool surface only makes the agent work around it",
      "Limit and scope access, require auth, grant least privilege, and confirm destructive actions",
      "Trust that the model never chooses to do anything dangerous without a good reason",
      "Hide the dangerous tools by leaving them out of the description the model sees",
    ],
    c: 1,
    e: "Least privilege and explicit confirmation on destructive actions. You design for things going wrong — not for the model always behaving.",
  },
  {
    d: "d4",
    q: "In MCP: what does the host/client side do, and what exposes the tools?",
    a: [
      "The server connects to clients, which are the ones that actually provide the model itself",
      "The host/client connects to servers; the servers expose the tools and resources",
      "Both sides do the same thing — the distinction is just a name with no practical meaning",
      "The client exposes the tools, while the server runs the language model itself",
    ],
    c: 1,
    e: "The host app (the client) connects to one or more MCP servers. It's the servers that offer the tools and resources the model can use.",
  },
  {
    d: "d4",
    q: "You've built many overlapping tools. What's the main risk?",
    a: [
      "Higher server cost, because each extra tool has to be kept running all the time",
      "The model picks the wrong tool — keep them distinct, well-scoped and clearly named",
      "Slower responses, since the model has to load all the tools before every single call",
      "No real risk — the more tools the model has, the better it solves tasks",
    ],
    c: 1,
    e: "Overlapping tools confuse the choice. Few, distinct, clearly named tools give more reliable selection than a large, fuzzy arsenal.",
  },
  {
    d: "d4",
    q: "A tool returns an enormous payload. What's the architectural concern?",
    a: [
      "It looks messy in the log, but doesn't affect how the model actually works",
      "It eats context and tokens — return only what's needed, paginate or summarize",
      "Nothing — more data always makes the model respond faster and more precisely",
      "The model automatically ignores what it doesn't need, so the size is irrelevant",
    ],
    c: 1,
    e: "Everything the tool returns ends up in the context and costs tokens and focus. Return what's relevant, paginate or summarize — don't dump raw data.",
  },

  // D5 — Context (5)
  {
    d: "d5",
    src: "AR5-15, AR5-01",
    q: "A support agent handles multi-turn billing disputes. To keep the window manageable the team summarizes older turns, and as the conversation grows each summary is folded into the next one. Reviewers now find the agent quoting a disputed amount as \"a few hundred\" and a deadline as \"later this month\", where both were stated exactly in turns that have since been summarized. What fixes this?",
    a: [
      "Extract the transactional facts into a persistent case-facts block re-sent with every prompt, outside the summarized history",
      "Instruct the summarizer to preserve every numerical value and date verbatim inside the summary it writes for each block of older turns",
      "Give the agent a tool that reads the earlier turns back out of the application's stored transcript, and have it call that tool whenever a figure it needs is missing from the summary",
      "Raise the threshold that triggers summarization, so more original turns stay in the window before any compression happens",
    ],
    c: 0,
    e: "Guide 5.1 names the risk precisely: amounts, percentages, dates and customer-stated expectations are what condense into vague summaries. Preserving figures inside the summary only defers the loss: a summary is dependable for what was found and not for the exact form it was stated in, and each summary here is folded into the next, so the instruction has to survive every pass. A retrieval tool conditioned on noticing the gap never fires — a vague summary reads as complete, so nothing prompts the call. And raising the threshold buys turns, not durability. A block held outside summarized history and re-sent with each prompt carries the facts verbatim however often the history is re-summarized.",
  },
  {
    d: "d5",
    src: "AR5-04, AR5-78",
    q: "A research subagent has sixty file excerpts to hand to the synthesis agent, which wants them all as evidence and has room for them, and which will read them alongside its own instruction. Several of the excerpts carry the findings that matter most. How should the subagent arrange what it hands over?",
    a: [
      "Lead with a summary of the key findings, and put the excerpts themselves after it",
      "Keep the excerpts in the order they were read, so the synthesis agent can follow how the investigation went and judge for itself which findings mattered",
      "Note why each excerpt matters in short lines between the excerpts themselves, so the reasoning sits next to what it refers to",
      "Hand over only the excerpts that carry the key findings and leave the rest out, so nothing unimportant competes for attention",
    ],
    c: 0,
    e: "A model processes the beginning and end of a long input reliably and may omit what sits in the middle, so the findings are safest gathered where they cannot be skipped. Reading order is the investigation's structure rather than the reader's, and it scatters the important excerpts wherever they happened to turn up. Annotating each excerpt in place keeps every finding beside its evidence and puts the annotations themselves through the middle of the input, which is the region read least reliably. And dropping the unremarkable excerpts answers a question nobody asked: the synthesis agent wants all sixty as evidence and has room for them, so the problem was never how much to send.",
  },
  {
    d: "d5",
    src: "AR5-07",
    q: "What does token budgeting mean in a multi-step workflow?",
    a: [
      "Counting the tokens each step consumes so the total can be reported after the run",
      "Deliberately allocating the context — input, reasoning and output — and trimming between steps",
      "Using as few models in the chain as possible, so total token consumption stays predictable",
      "Setting max_tokens as high as the model allows so no step's answer can be truncated",
    ],
    c: 1,
    e: "You treat the context as a budget you allocate deliberately across the steps, and clean up between them. Otherwise it grows until it hits the ceiling and breaks down.",
  },
  {
    d: "d5",
    src: "AR5-34",
    q: "A coordinator assigns one jurisdiction each to five research subagents, and the decomposition is right — every jurisdiction in scope has an agent. Four return findings drawn from the regulators' own published guidance. The fifth's regulator portal was offline for the whole run, so that agent returns findings drawn from a single secondary article. The synthesis step writes all five up in the same voice. What should the synthesis output do instead?",
    a: [
      "Annotate each jurisdiction with how well supported it is, and say which rest on gaps left by sources that could not be reached",
      "Have the coordinator record the offline portal in a methodology note at the end of the report, stated once for the run as a whole without singling out which of the five it bears on",
      "Order the jurisdictions by how many sources back each one, so a reader can see the strength of support from the sequence",
      "Leave the fifth jurisdiction out of the review, so nothing published rests on a single secondary source",
    ],
    c: 0,
    e: "What the review owes its reader is which findings rest on sources that could not be reached, and why — that is the content of a coverage annotation, and without it the fifth jurisdiction is indistinguishable from the four that rest on primary guidance. Ordering by source count conveys relative strength while saying nothing about where the holes are or what caused them. Dropping the jurisdiction destroys the only work done on it and leaves the reader unaware there was a fifth. A methodology note that names the outage but not which jurisdiction it cost leaves every finding unqualified, which is the half of the annotation the guide actually asks for.",
  },

  /* ===== Extended set ===== */

  // D1 — Agentic (10 more)
  {
    d: "d1",
    q: "A complex task has several clearly separate steps. What gives the most reliable result?",
    a: [
      "One big prompt that asks the model to work through all the steps at once in a single call",
      "Prompt chaining — break it into discrete steps, each with scoped responsibility and validatable",
      "Let an autonomous agent improvise freely through the steps with no fixed structure",
      "Run the same prompt many times and let the majority of answers decide the result",
    ],
    c: 1,
    e: "Prompt chaining trades a little latency for a lot of reliability: each step is simpler, easier to verify, and errors are caught where they occur instead of propagating through one unwieldy call.",
  },
  {
    d: "d1",
    src: "AR1-146",
    q: "Your coordinator's synthesis subagent returns a report in which two of the requested areas are barely covered. What should the coordinator do with it?",
    a: [
      "Evaluate the synthesis for gaps, send targeted queries back to the search and analysis subagents, then re-synthesize",
      "Ask the search and analysis subagents for more on the two thin areas, then append what they return to the report as it stands",
      "Name the two thin areas for the search and analysis subagents, take what comes back, and re-run the synthesis call",
      "Re-run just the two thin areas through synthesis, leaving the rest of the report as it already stands",
    ],
    c: 0,
    e: "Aggregating what the subagents returned is not the end of the coordinator's job. It evaluates the synthesis it got back, sees which requested areas are thin, sends targeted queries to the agents that gather, and synthesizes again — repeating until the coverage is sufficient. A second synthesis pass cannot add what the findings do not contain, which is why the loop has to reach back past synthesis rather than around it. Asking the gathering agents for more and appending what comes back does the first two steps and stops short of the third, leaving a stitched addendum where a synthesis was wanted; naming the thin areas and re-running synthesis once is a single corrective pass rather than a loop that repeats until the coverage holds.",
  },
  {
    d: "d1",
    src: "AR1-113",
    q: "Your team keeps arguing about when to have Claude write a plan before it changes anything. Some changes clearly warrant one and some clearly do not. What is the test?",
    a: [
      "Whether the request as written ran to more than one sentence",
      "Whether the diff it produces could be described in one sentence",
      "Whether the change touches more than one file in the repository",
      "Whether the code being changed is code the team has worked in before",
    ],
    c: 1,
    e: "The test is about the size of the change, not the size of the ask. Planning earns its cost when the approach is uncertain, when the change touches several files, or when the code is unfamiliar — three conditions, any of which is enough. The compact form of the same rule is the one-sentence test: if you could describe the diff in a sentence, the plan is overhead. A long request can still produce a one-line diff, and file count or unfamiliarity each stands in for only one of the three conditions.",
  },
  {
    d: "d1",
    q: "An agent can perform an irreversible action (e.g. send money). Where should a human-in-the-loop checkpoint go?",
    a: [
      "After the action is performed, as a log of what the agent actually did",
      "Before the irreversible action is performed — require explicit approval right there",
      "No checkpoint is needed as long as the model is large and capable enough",
      "Only when the user has explicitly asked to approve actions of that kind in advance",
    ],
    c: 1,
    e: "The checkpoint belongs in front of the irreversible step. Approval after the fact is just a log of something you can no longer stop.",
  },
  {
    d: "d1",
    q: "A business rule MUST apply every time, without exception. Where do you put it?",
    a: [
      "In the prompt, worded clearly enough that the model follows it every single time",
      "In deterministic code around the model, not left to the model's own judgment",
      "As an example in the prompt that the model can imitate in similar cases",
      "In CLAUDE.md, where it's loaded automatically and therefore always applies to the call",
    ],
    c: 1,
    e: "If something must hold absolutely, it can't be left to a probabilistic model. Critical rules belong in code; the model is used where judgment is actually wanted.",
  },
  {
    d: "d1",
    q: "In an agent loop — what's the correct order per step?",
    a: [
      "Call several tools in sequence and read all the results together at the end of the loop",
      "Observe the previous tool result, reason, and choose the next action — then repeat",
      "Choose all the actions in advance and run them through in a fixed order",
      "Skip the result from the previous call and go straight to the next call",
    ],
    c: 1,
    e: "A working agent loop is observe → think → act. Firing off the next call without reading the previous result is acting blind — the agent can't course-correct then.",
  },
  {
    d: "d1",
    src: "AR1-72",
    q: "A customer writes in with three things at once: a duplicate charge, an order that never arrived, and a question about closing the account. What should the agent do with the request?",
    a: [
      "Handle the duplicate charge now and open linked tickets for the other two, each with its own owner",
      "Split it into the three items, investigate them in parallel on one record, then answer once",
      "Split it into the three items and investigate each in a session of its own, then answer once",
      "Take the three in the order the customer raised them, resolving each before opening the next",
    ],
    c: 1,
    e: "A compound request is one customer's situation. The three concerns are separated into distinct items and investigated at the same time against the same record, then answered as a single resolution rather than three. Giving each its own session is the opposite move: the duplicate charge and the missing order are probably the same incident, and both investigations need the same order history. Isolation is about what a subagent inherits from its parent, not about whether one customer's concerns belong in one investigation. Sending the customer away to file two more tickets pushes the decomposition onto them, and picking an order of precedence answers a question nobody asked.",
  },
  {
    d: "d1",
    q: "How do you maintain state across turns in an agent flow?",
    a: [
      "The model remembers on its own what was said earlier in the conversation between each call",
      "You pass the relevant state and history explicitly into each call — the model is stateless",
      "State is stored automatically in the model's weights as the conversation develops",
      "It isn't possible in practice to retain state in an agent across multiple turns",
    ],
    c: 1,
    e: "The model has no memory between calls. Any state you need going forward you must carry explicitly in the context for the next call.",
  },
  {
    d: "d5",
    src: "AR5-17, AR5-05",
    q: "A synthesis agent must keep attributing every statement it writes, and it is doing so correctly today. Its context budget is small, though, and it receives four agents' full outputs — each a reasoning transcript plus every passage that agent read — so it truncates before it reaches the fourth. What should change?",
    a: [
      "Have each agent return key facts with their citations and relevance scores, rather than verbose content and its reasoning chain",
      "Have the coordinator read each agent's transcript and pull out the key facts and citations itself, handing the synthesis agent a condensed brief rather than the raw outputs",
      "Move the synthesis step to a model with a larger context window, and leave what the four agents produce exactly as it is",
      "Run the four agents one after another, so only one of their outputs is in the synthesis agent's window at a time",
    ],
    c: 0,
    e: "The constraint is purely how much the synthesis agent must hold at once, and the guide names where to fix it — the agents producing the output, not the consumer of it. Having the coordinator condense the transcripts leaves the verbose form still being produced and moves the reading cost onto another agent. A larger window buys room without improving what fills it, and accuracy and recall degrade as the context grows. Sequencing changes the order the outputs arrive in, not the total. What changes upstream is the shape of the return value: facts and their sources instead of the reasoning that found them.",
  },

  // D2 — Claude Code (7 more)
  {
    d: "d2",
    src: "AR2-08",
    q: "Why should CLAUDE.md be kept concise and high-signal?",
    a: [
      "Because long files are cumbersome to commit and create unnecessary merge conflicts on the team",
      "Because it sits in the context all the time — bloated content costs tokens and dilutes what matters",
      "Because Claude only reads the first lines of the file and skips the rest anyway",
      "It doesn't really matter how long it is, as long as the content is actually correct",
    ],
    c: 1,
    e: "CLAUDE.md is always in the context. If you fill it with everything, the important conventions drown and you pay tokens for noise on every single call.",
  },
  {
    d: "d2",
    src: "AR2-45",
    q: "You want to delegate a scoped subtask without filling the main conversation with noise. What fits?",
    a: [
      "Paste everything relevant into the main conversation so Claude has full visibility at all times",
      "A subagent that solves the subtask in its own context and returns only the result",
      "Restart the whole project with the subtask as the very first priority",
      "Temporarily turn off CLAUDE.md so the context doesn't fill up while you work",
    ],
    c: 1,
    e: "A subagent gets its own context for the subtask and delivers a result back. The main conversation stays clean for what it's actually about.",
  },
  {
    d: "d2",
    src: "AR2-02, AR2-05",
    q: "You have conventions that apply to the whole repo, and some that apply to just one subfolder. How is that best solved?",
    a: [
      "Everything in one CLAUDE.md at the root, with headings marking which folder each section governs",
      "A CLAUDE.md at the root for the shared parts, and a more specific one in the subfolder for the local parts",
      "Repeat the whole set of conventions in a separate CLAUDE.md in every single folder of the repo",
      "Put the subfolder rules as comments in the code files where they are meant to apply",
    ],
    c: 1,
    e: "CLAUDE.md files can live in a hierarchy. The general stuff belongs at the root; what applies only to part of the codebase goes closer to where it applies.",
  },
  {
    d: "d4",
    q: "An MCP server setup should be available to the whole team in one specific project. Which scope?",
    a: [
      "User scope, which makes the server available to you in every project without touching the repo",
      "Project scope committed to the repo, so everyone on the project gets the server where it belongs",
      "A machine-wide scope so every session on the host gets it and nobody can disable it",
      "MCP servers cannot be shared through the repo, so each developer configures it locally",
    ],
    c: 1,
    e: "What makes project scope shared is the file: .mcp.json sits at the project root and goes into version control, so the server arrives with a clone instead of being set up once per machine. It is also a deliberate choice rather than the default, since an unqualified claude mcp add writes local scope, which lives in your own ~/.claude.json and loads only in the project where you ran it.",
  },
  {
    d: "d1",
    q: "What's the practical difference between a hook and an instruction in CLAUDE.md?",
    a: [
      "No real difference — both control Claude Code's behavior in exactly the same way",
      "A hook is code that's guaranteed to run; CLAUDE.md is guidance the model can deviate from",
      "CLAUDE.md runs code automatically, while a hook is just plain text the model reads",
      "Hooks apply only to design tasks, while CLAUDE.md applies only to pure coding tasks",
    ],
    c: 1,
    e: "If you need a guarantee, use a hook — it runs no matter what. CLAUDE.md shapes behavior, but it's still instructions a probabilistic model can interpret or slip on.",
  },

  // D3 — Prompt engineering (7 more)
  {
    d: "d3",
    q: "You want consistent format and behavior on a tricky edge case. What helps most?",
    a: [
      "Ask the model to 'be accurate' and pay extra attention to the difficult special cases",
      "Few-shot — show a few concrete examples of the desired input→output, including the edge case",
      "Turn up temperature so the model explores more ways to handle the case",
      "Make the prompt shorter so the model isn't distracted by too many details at once",
    ],
    c: 1,
    e: "Examples steer more strongly than explanations. A couple of good few-shot examples — especially of the tricky case — show the model precisely what you want, instead of it having to guess.",
  },
  {
    d: "d3",
    q: "What gives the most reliable behavior?",
    a: [
      "Vague instructions that leave the model free to interpret the task however it likes",
      "Explicit, specific instructions about exactly what you want and how",
      "Assuming the model already knows your context and preferences in advance",
      "Leaving out format requirements so the model picks the format it thinks fits best",
    ],
    c: 1,
    e: "The model doesn't read your mind. Whatever you leave unspecified, it fills in by its own judgment — be explicit about format, scope and requirements when it matters.",
  },
  {
    d: "d5",
    src: "AR5-06, AR5-04",
    q: "An analyst pastes a 90-page supplier contract into one prompt and asks a single question about its termination clause. Where should the question go relative to the contract?",
    a: [
      "After the contract, so the whole document sits above the question in the prompt",
      "Before the contract, so the model knows what it is looking for while it reads the document through",
      "Once above the contract and once below it, so the question frames the reading and is also the last thing read",
      "Split across the contract, with a part of the question after each section it might bear on",
    ],
    c: 0,
    e: "For a long document and a question about it, Anthropic's guidance is to put the longform data at the top, above the query. The end of a long prompt is read as reliably as the beginning, so nothing is lost by asking last, while leading with the question reads naturally and measures worse. Asking twice is not a safe middle course: the copy above the contract is the placement the guidance avoids, and the model does not read the contract once per copy of the question. Splitting the question up scatters the one thing that should be easy to find into the region of a long input that is least reliably used.",
  },
  {
    d: "d3",
    q: "What's an effective way to lock the output to a specific format?",
    a: [
      "Hope the model hits the right format from the context it gets in the question",
      "Give an explicit template or structure, and optionally prefill the start of the answer",
      "Ask for the answer to be 'nice' and neatly formatted when delivered",
      "Set temperature to 1 so the model finds the format that fits best itself",
    ],
    c: 1,
    e: "Show the shape you want — a template, a skeleton, or a prefill of the start. Concrete structure produces consistent output; 'nice' is not a specification.",
  },
  {
    d: "d3",
    q: "What does the temperature parameter control?",
    a: [
      "How long the answer is, by controlling how many tokens the model is allowed to use",
      "The degree of randomness — low gives more consistent answers, high gives more variation",
      "How many tools the model has access to during a single call",
      "The size of the context window and how much the model can read at once",
    ],
    c: 1,
    e: "Temperature is the randomness dial. Low for tasks that need consistency and precision; higher for brainstorming and variation.",
  },
  {
    d: "d3",
    q: "Why set a clear role in the system prompt (e.g. 'you are an experienced auditor')?",
    a: [
      "It makes the answers longer because the model explains more when given a role",
      "It frames the domain, tone and expectations, and raises the relevance of the answers",
      "It's a required field in the API that the call simply fails without",
      "It reduces token cost by cutting down how much the model has to read",
    ],
    c: 1,
    e: "A precise role gives the model a vocabulary and a perspective to answer from. It steers both content and tone toward what you actually need.",
  },
  {
    d: "d3",
    q: "A complex prompt mixes instructions, context, examples and the input itself. What helps?",
    a: [
      "Write it all in one long, flowing sentence so it hangs together as naturally as possible",
      "Split it into clearly labeled sections, for example with XML tags, so the model sees what's what",
      "Remove all structure and let the model figure out the connections entirely on its own",
      "Send just the input without instructions, so the model isn't confused by requirements",
    ],
    c: 1,
    e: "Clear sectioning — instruction, context, examples, input each on their own — keeps the model from confusing your data with the task. XML tags are a common trick for exactly this.",
  },

  // D4 — Tool/MCP (7 more)
  {
    d: "d4",
    q: "An MCP server wants to offer read-only reference data the model can look things up in. Which primitive fits?",
    a: [
      "A tool, since anything the model fetches from the server counts as an action it performs",
      "A resource — contextual, read-only data the server exposes to the model",
      "A hook that runs and pulls the data in automatically before every call to the model",
      "A plugin that bundles the reference data together with the commands that use it",
    ],
    c: 1,
    e: "MCP separates tools (actions the model can perform) from resources (data it can read). Static reference material fits as a resource; actions with effects are tools.",
  },
  {
    d: "d4",
    src: "AR4-08",
    q: "Two tools have distinct, carefully written descriptions with explicit boundaries, yet the agent keeps routing questions about the onboarding documentation to search_tickets. The system prompt opens with \"Whenever the user mentions an issue, search the ticket system first.\" What is the most likely cause?",
    a: [
      "The tool schemas are too loose, so adding strict: true to both would make selection reliable again",
      "Neither tool is namespaced, so the model can't tell which domain each one belongs to",
      "The system prompt's keyword-sensitive wording creates a tool association that overrides what the descriptions say",
      "Tool descriptions are advisory, so selection has to be steered with an instruction to always call a tool first",
    ],
    c: 2,
    e: "Guide 2.1 pairs this as knowledge and as a skill: keyword-sensitive instructions build tool associations of their own, and the architect's move is to read the system prompt for them first. A description cannot outrank an instruction that names the trigger word, strict: true governs the shape of arguments rather than which tool is chosen, and namespacing draws boundaries between tools.",
  },
  {
    d: "d4",
    q: "How should a tool handle an error (e.g. invalid input)?",
    a: [
      "Return nothing and let the model notice on its own that something went wrong with the call",
      "Return a structured, descriptive error message the model can understand and correct against",
      "Crash the whole agent so the error can't propagate further in the run",
      "Pretend everything went fine and return an empty but technically valid result",
    ],
    c: 1,
    e: "A good error message is actionable for the model: it says what was wrong, so the agent can correct and retry. Silent failures leave the model blind.",
  },
  {
    d: "d4",
    src: "AR4-55, AR4-56",
    q: "Your research agent has a fetch_url tool that takes a URL and returns what is at it. A steady share of calls come back as pages the extraction step cannot parse, and each one has already cost a round trip and a slice of context. How should the tool change?",
    a: [
      "Add a required content_type parameter the model has to declare before the fetch runs",
      "Narrow it to a load_document tool that rejects a URL whose type the pipeline cannot parse",
      "Add a validate_url tool and instruct the agent to call it ahead of every fetch",
      "Split it into a search tool returning document ids and a load tool that takes one of those ids",
    ],
    c: 1,
    e: "The objective's own example is this one: replace a generic fetch_url with a load_document that validates document URLs. A tool's own interface is the constraint the model cannot talk its way past, so the check belongs inside the call that would otherwise waste the round trip. A content_type parameter asks the model to know the answer before it has fetched anything. A separate validator the agent is told to call first is an instruction rather than a contract, and it doubles the calls when it is obeyed. Routing everything through ids is a sound design elsewhere, but it costs a research agent the ability to follow a URL it found while reading.",
  },
  {
    d: "d4",
    q: "How do you reduce invalid arguments to a tool?",
    a: [
      "Make all parameters free strings so the model is completely free to fill them in",
      "Use precise types and enums in the schema where the values are known and limited",
      "Drop the schema entirely and instead validate the arguments inside the tool code itself",
      "Ask the model to be extra careful when filling in the tool's parameters",
    ],
    c: 1,
    e: "A tight schema makes invalid calls harder. Enums and precise types limit what the model can send, so errors are caught by the contract instead of at runtime.",
  },
  {
    d: "d4",
    q: "When should something be exposed as a resource rather than a tool in MCP?",
    a: [
      "Almost always a resource, since tools are reserved for a few rare special cases",
      "When it's data to read or reference; tools are for actions that actually do something",
      "Almost always a tool, because the model works best when absolutely everything is an action",
      "It's arbitrary which you choose — MCP treats resources and tools exactly the same anyway",
    ],
    c: 1,
    e: "Rule of thumb: data you read → resource; an action that has an effect → tool. The distinction helps both the model and you keep track of what's safe to fetch and what changes something.",
  },
  {
    d: "d4",
    q: "An MCP server needs to authenticate against an external API. Where do the credentials belong?",
    a: [
      "Passed through the model's context in plain text so it can use the key when needed",
      "Handled on the server and integration side — not routed through the model's context",
      "Written into every prompt so the right key is always available for the call",
      "Stored in the tool description the model reads before calling the tool itself",
    ],
    c: 1,
    e: "Secrets should never pass through the model's context. Auth is handled where the server talks to the external system, outside of what the model sees.",
  },

  // D5 — Context (5 more)
  {
    d: "d5",
    src: "AR5-23, AR5-22",
    q: "A customer opens with \"honestly this is ridiculous\" and describes a duplicate charge — a case the agent can refund on its own. The customer has not asked for a human. What should the agent do?",
    a: [
      "Acknowledge the frustration and offer to resolve it now, handing off only if the customer then asks for a person",
      "Resolve the duplicate charge without commenting on the tone, since the refund is what the customer actually needs and the tone will look handled once the money is back",
      "Escalate now, since the opening line is plainly angry and a person will settle it better than another agent turn would",
      "Ask the customer whether they would rather deal with a person before doing anything about the charge",
    ],
    c: 0,
    e: "The condition that decides this is whether the agent can actually settle the case, and here it can — the refund is squarely within what its own policy lets it do, which is what makes offering to resolve the right move rather than handing off. A handoff spends a person on a case that needed none. Fixing the charge without a word is closer, but it answers only half of what the customer said. And offering the handoff unprompted invites one nobody asked for: the agent waits for the customer to actually ask for a person.",
  },
  {
    d: "d5",
    src: "AR5-05",
    q: "A large context window means you should fill it completely. True or false?",
    a: [
      "True — more context always gives better answers, so fill as much space as you can",
      "False — excess context can dilute what's relevant and cost unnecessarily, so be selective",
      "True, as long as all the content actually fits within the window's limit",
      "False, because the model ignores everything except the very first tokens anyway",
    ],
    c: 1,
    e: "Space is not an obligation to fill it. Irrelevant filler competes with the important stuff for the model's attention and costs tokens — quality of context beats quantity.",
  },
  /* ===== Additions from the official Anthropic Exam Guide audit (2026-08-21) ===== */

  // D1 additions
  {
    d: "d1",
    q: "Your agent loop just received a response from Claude with stop_reason set to \"tool_use\". What should the loop do next?",
    a: [
      "Execute the requested tool call(s), append the results to the conversation, and send another request",
      "Treat this as the final answer and return it to the user, since the model produced output",
      "Ignore stop_reason and decide whether to continue based on whether the response contains any text",
      "Wait for the user to manually confirm the tool call before doing anything else",
    ],
    c: 0,
    e: "\"tool_use\" means Claude needs a tool executed before it can finish, so the loop runs the requested tool(s), feeds the results back into the conversation history, and sends the next request. The loop only terminates once a response comes back with stop_reason \"end_turn\". Deciding termination by scanning for text content or phrases is exactly the unreliable signal this field replaces.",
  },
  {
    d: "d1",
    q: "A coordinator agent is supposed to spawn subagents to handle parts of a request, but every attempt fails with a permissions error before any subagent even starts. Its tool config lists file and web-search tools but nothing else. What's missing?",
    a: [
      "'Task' needs to be added to the coordinator's allowedTools — it's the mechanism for spawning subagents",
      "The subagents' own allowedTools need to include 'Task' before they can accept being spawned at all",
      "AgentDefinition needs to be disabled for the coordinator so it can call tools freely",
      "Nothing — subagent spawning doesn't go through the normal tool permission system at all",
    ],
    c: 0,
    e: "Spawning a subagent goes through the Task tool, so the coordinator's allowedTools must explicitly include \"Task\" or every invocation attempt is blocked. This permission lives on the coordinator doing the spawning, not on the subagents being spawned. Without it, delegation is impossible no matter how the subagents themselves are configured.",
  },
  {
    d: "d1",
    q: "You've built up a solid, expensive-to-repeat analysis of a codebase in one session. Now you want to try two different refactoring strategies from that same starting point, without one exploration's findings bleeding into the other's context.",
    a: [
      "Use fork_session to branch two independent sessions off that shared analysis baseline",
      "Spawn one subagent per strategy, since each inherits the parent's analysis and explores from there",
      "Paste the full transcript into two brand-new sessions and proceed from there",
      "Run both strategies sequentially in the same session, then discard the one you don't want",
    ],
    c: 0,
    e: "fork_session exists for exactly this: branching independent sessions from a shared analysis baseline to explore divergent approaches without cross-contaminating context. --resume continues a single named conversation rather than splitting it into parallel branches. Manually copying transcripts or reusing one session for both strategies both risk the explorations bleeding into each other.",
  },
  {
    d: "d1",
    q: "You paused a named investigation session three days ago and want to continue that exact conversation today, not start over.",
    a: [
      "Continue it with --resume <session-name>",
      "Start a fresh session and rely on fork_session to pull in the old context",
      "Sessions aren't individually addressable, so you'd have to reconstruct the context manually",
      "Use AgentDefinition to reload the prior conversation by name",
    ],
    c: 0,
    e: "Named session resumption via --resume <session-name> is built for continuing one specific prior conversation across work sessions. fork_session branches off into a new, divergent exploration rather than resuming the same thread. If the session's prior tool results are still valid, resuming by name is more reliable than reconstructing context from scratch.",
  },
  {
    d: "d1",
    src: "AR1-58, AR1-59",
    q: "You define a doc-writer subagent in the Agent SDK to draft release notes, and you leave the tools field out of its AgentDefinition, assuming a subagent starts from a minimal default. On its first run it edits two source files. What happened?",
    a: [
      "The subagent inherited the parent session's tool permissions along with the rest of the parent's context",
      "Leaving the tools field out hands the subagent every tool available to subagents, restricting nothing",
      "An empty tools field is read as a request for the built-in file tools, which include Write and Edit",
      "The tools field filters MCP tools only, so the built-in file tools stay available whatever it says",
    ],
    c: 1,
    e: "The tools field is an allowlist that only exists once you write it, so omitting it is the maximally permissive choice rather than the safe default. The restriction also fails quietly in the other direction: a tool you leave off the list is simply absent from the subagent's session, with no permission prompt and no error, so the subagent works without it and you see nothing. Nothing about this is inheritance — the parent's conversation and tool results never cross into a subagent either way.",
  },
  {
    d: "d1",
    q: "An agent needs to escalate a mid-process customer issue to a human agent who won't see the conversation transcript. What must the handoff summary include to actually be usable?",
    a: [
      "Just the customer's original message, since the human agent can ask follow-up questions themselves",
      "Customer details, the root cause analysis, and a recommended action",
      "A copy of the full system prompt so the human understands how the agent was configured",
      "Nothing structured is needed — a short note that escalation happened is enough",
    ],
    c: 1,
    e: "Because the human agent lacks access to the conversation transcript, the handoff has to carry what they'd otherwise be missing: who the customer is, what actually went wrong, and what to do about it. A bare escalation note or just the original message leaves the human re-investigating from scratch, and the system prompt isn't useful context for someone picking up the case.",
  },
  {
    d: "d1",
    q: "Your agent calls three different MCP tools, and each returns dates in a different format — one as a Unix timestamp, one as ISO 8601, one as a numeric status code that means something else entirely. What's the cleanest fix?",
    a: [
      "Add a PostToolUse hook that normalizes each tool's output into a consistent format before the model sees it",
      "Tell the model in the prompt to interpret whichever date format shows up, case by case, as it reads each result",
      "Reject any tool result that is not already in ISO 8601 and ask the server to fix its output",
      "Add a conversion step the model runs after each call, turning every value into one format",
    ],
    c: 0,
    e: "A PostToolUse hook intercepts tool results and can normalize heterogeneous formats — timestamps, status codes, whatever's inconsistent across tools — before the model has to reason about them. Leaving format interpretation to the model's judgment in the prompt is exactly the probabilistic handling this hook replaces, and rejecting valid results or adding an extra model-driven conversion step just adds cost and failure surface.",
  },
  {
    d: "d1",
    q: "Company policy caps any refund an agent can issue on its own at $500 — anything above that must go to a human. Where should this rule live?",
    a: [
      "In the system prompt, phrased as a hard limit the agent must not cross under any circumstances",
      "In a hook that intercepts the outgoing refund call and redirects to human escalation above the threshold",
      "As a line in the refund tool description, so the limit is stated where the agent reads it before acting",
      "Nowhere explicit — the model can reason about what counts as a reasonable refund from the case itself",
    ],
    c: 1,
    e: "A hard dollar threshold like this calls for a hook that intercepts the outgoing tool call, checks the amount, and redirects to escalation when it's over the limit — a deterministic guarantee. Prompt-based limits, tool descriptions, or trusting the model's judgment are all probabilistic and can be violated. A business rule with zero tolerance for exceptions belongs in code around the model, not in its instructions.",
  },
  {
    d: "d1",
    src: "AR1-66",
    q: "Your coordinator has three independent research subtasks and none of them depends on the others. In the trace it spawns the first subagent, waits for the result, spawns the second, waits again, then spawns the third. What should it do differently?",
    a: [
      "Emit all three spawn calls in a single response rather than one per turn",
      "Have the first subagent spawn the other two, so those two branches run underneath it",
      "Raise the coordinator's turn budget so it doesn't pause for a result between dispatches",
      "Merge the three subtasks into one subagent prompt so a single delegation covers them all",
    ],
    c: 0,
    e: "Parallelism here is a property of how the dispatch is written, not a setting you turn on: the calls have to leave the coordinator together, in one response, or each one becomes its own turn and the work serialises however independent the subtasks are. Nesting moves the dispatch down a level without making it simultaneous, a larger turn budget just permits more sequential turns, and folding three independent tracks into one prompt puts all three back on one agent's plate.",
  },
  {
    d: "d1",
    src: "AR1-145, AR1-40, AR1-41",
    q: "Four research subagents were each told to 'research the competitive landscape.' All four reports come back analysing the same three companies, and most of the work was spent twice over. What should have been done differently?",
    a: [
      "Give each subagent its own output file, so the overlap is visible when the reports are compared",
      "Give each subagent a distinct segment or source type, so no two cover the same ground",
      "Give the whole landscape to one subagent, since a single agent sees what it has already covered",
      "Give each subagent a company to lead on, so the four reports divide by who is named in them",
    ],
    c: 1,
    e: "Four agents given one undivided brief converge on the most obvious material, so the fix sits upstream of the work: partition the scope itself, by subtopic or by source type, so each agent's ground is its own. Separate output files address a different failure — two agents writing one file overwrite each other — and nothing here was overwritten; the waste is four agents researching the same companies. Giving each agent a company to lead on divides the three companies they already converged on rather than partitioning the landscape, and handing the whole landscape to one agent removes the overlap by removing the parallelism.",
  },

  // D2 additions
  {
    d: "d2",
    src: "AR2-06, AR2-07, AR2-08",
    q: "Your root CLAUDE.md is getting long, so you use @import to pull a package's coding standards in from a separate file instead of pasting them into the main file. Which two things are true of what you have just done? Select 2.",
    a: [
      "It keeps the main file modular by referencing the standards file instead of copying it in",
      "It does not reduce context, because imported files are loaded at launch alongside the file that references them",
      "The standards file is read only when Claude opens a file inside that package",
      "The import is resolved once and cached, so later edits to the standards file need a session restart",
      "An import inside a fenced code block is followed too, so paths in examples have to be escaped",
      "Claude re-reads the standards file at the start of each turn, so edits to it take effect immediately",
    ],
    c: [0, 1],
    e: "@import lets a CLAUDE.md reference an external file rather than duplicate it, which is genuinely useful for keeping a long file modular. What it does not do is save context: an imported file is expanded and loaded at launch alongside the CLAUDE.md that references it, so the tokens arrive either way. The documented remedy for a file that is growing too large is a path-scoped rule in .claude/rules/, which loads only when Claude works with matching files — that is the option that would make the standards load lazily, and @import is not it. Import parsing skips code spans and fenced blocks, so a path mentioned in an example is left alone rather than followed.",
  },
  {
    d: "d2",
    src: "AR2-08, AR2-28",
    q: "Your CLAUDE.md has grown into one long file mixing testing conventions, API conventions, and deployment steps, and it's becoming hard to maintain. What's a better structure for this?",
    a: [
      "Split it into topic-specific files like testing.md, api-conventions.md, and deployment.md inside .claude/rules/",
      "Move everything into CLAUDE.local.md, which loads alongside CLAUDE.md and stays out of version control",
      "Delete the least-used sections to bring the file under the 200-line target the docs recommend",
      "Turn the conventions into a slash command so they load only when the command is explicitly invoked",
    ],
    c: 0,
    e: ".claude/rules/ exists specifically as an alternative to a monolithic CLAUDE.md, letting you split content into focused topic files such as testing.md, api-conventions.md, and deployment.md. Renaming to CLAUDE.local.md only changes who it's shared with, deleting content loses information rather than organizing it, and CLAUDE.md content isn't the kind of thing you convert into a slash command.",
  },
  {
    d: "d2",
    src: "AR2-13, AR2-14",
    q: "A developer wants the team's shared /deploy skill to behave slightly differently for them, so they put an edited version at ~/.claude/skills/deploy/SKILL.md, keeping the same name. Which two consequences follow? Select 2.",
    a: [
      "Their /deploy now resolves to the personal file, so later changes the team makes to the project skill no longer reach them",
      "The personal skill is active in every project they open on that machine, not only this repository",
      "Claude Code reports a name collision at startup and refuses to load either skill until one of the two is renamed",
      "The project skill still wins, because a skill committed to the repository outranks one that exists only locally",
      "The two files are merged, with the personal file's frontmatter taking precedence over the project file's field by field",
      "Their edited version is shared back to the team the next time they commit, since skills travel with the repository",
    ],
    c: [0, 1],
    e: "Personal skills sit above project skills in the resolution order, so a personal file with the same name shadows the team's rather than conflicting with it — the collision is silent, and it is why the guidance is to give a personal variant a different name. The second consequence is easy to miss: ~/.claude/skills/ is not tied to one checkout, so the shadowing follows them into every codebase they open on that machine. Nothing merges the two files, neither is refused, and a file under the home directory is outside the repository, so committing does not share it.",
  },
  {
    d: "d2",
    src: "AR2-15",
    q: "A skill you built runs a deep codebase analysis and produces a lot of exploratory output you don't want cluttering the main conversation. Which SKILL.md frontmatter option addresses this?",
    a: [
      "allowed-tools, to restrict which tools the skill can call",
      "argument-hint, to prompt for the arguments the skill needs",
      "context: fork, to run the skill in an isolated sub-agent context",
      "A longer description field documenting how verbose the skill is",
    ],
    c: 2,
    e: "context: fork runs a skill in an isolated sub-agent context, so verbose or exploratory output like a codebase analysis doesn't pollute the main conversation. The other frontmatter fields address different problems entirely: two of them concern what the skill may do and what it should be invoked with, and the description field is documentation. None of them changes where the skill's output lands.",
  },
  {
    d: "d2",
    src: "AR2-17",
    q: "You're writing a skill that should only ever write files — never run shell commands or hit the network — no matter what a later prompt tries to get it to do. What actually enforces that?",
    a: [
      "A strongly worded instruction in the skill telling it never to shell out",
      "Setting allowed-tools in the skill's frontmatter to restrict tool access",
      "Setting context: fork, since a separate context cannot reach the network",
      "Adding an argument-hint so callers see what the skill expects",
    ],
    c: 1,
    e: "allowed-tools in skill frontmatter is the field that governs which tools are available while the skill runs, so it is the mechanism rather than an instruction the model may or may not follow. Two details worth carrying: whatever it sets applies to the turn that invokes the skill and clears on your next message, so it is not a standing policy for the session; and current Claude Code documentation frames the field as pre-approving tools rather than as removing them, which is the same field described from the other side. A prompt warning is still only instruction, context: fork isolates conversation context rather than tool access, and argument-hint has nothing to do with tools at all.",
  },
  {
    d: "d2",
    src: "AR2-24, AR2-25",
    q: "Two files sit in .claude/rules/: security.md, whose frontmatter has no paths field, and terraform.md, whose frontmatter sets paths: [\"terraform/**/*\"]. A developer editing a React component reports that the security rules are in play and the Terraform ones never are. What explains it?",
    a: [
      "Both rules load at launch, but a glob is re-checked on every tool use, so the Terraform rule drops out of context whenever the file being touched is not Terraform",
      "An unscoped rule is loaded at a higher priority than a scoped one, so security.md wins wherever the two would both apply",
      "security.md has no paths field, so it loads at launch for every session; terraform.md only enters context when Claude reads a file its glob matches",
      "A rule's glob is matched against the directory the session was started in, so terraform.md never loads in a session opened at the repo root",
    ],
    c: 2,
    e: "Two files, two load behaviours, and the developer is watching both work correctly. An unconditional rule comes in at launch and stays for the session, ranked alongside the project's own CLAUDE.md — so security conventions are present on a React file whether or not anyone wanted them there. Scoping by path changes when the instructions arrive, not how strongly they apply: the trigger is Claude reading a matching file, which is why a scattered file type can carry its own conventions without costing context on every unrelated task. It is a trigger, not a filter re-evaluated per tool use, and the glob is matched against files being read rather than the directory the session started in.",
  },
  {
    d: "d2",
    src: "AR2-30, AR2-32, AR2-33",
    q: "You investigated a library migration in plan mode, Claude has produced a plan, and you choose one of the approve options. Which two things are true of what happens next? Select 2.",
    a: [
      "The session leaves plan mode and switches to the editing permission mode the approve option named",
      "To plan again later, you re-enter plan mode with Shift+Tab or by prefixing a prompt with /plan",
      "The session stays in plan mode, and each edit is put to you for approval as it comes up",
      "The approved plan is written to disk so a later session can pick it up where this one stopped",
      "Claude re-checks the plan against the codebase before every subsequent turn",
      "The block on edits holds until you leave plan mode yourself with Shift+Tab",
    ],
    c: [0, 1],
    e: "Approving is a mode change, not a queue of pending edits: the session exits plan mode and enters whichever editing mode the option you picked describes, so Claude starts changing files from that point. That is what makes the guide's investigate-then-implement pattern work — plan mode for the research, an ordinary editing mode for the execution. It also means planning is not a state you stay in: to plan the next piece of work you deliberately go back, with Shift+Tab or a /plan prefix. Nothing persists the plan to disk for another session, and nothing re-validates it turn by turn.",
  },
  {
    d: "d2",
    src: "Exam Guide v1.0 §6 3.5",
    q: "You're about to implement a caching layer in a domain you don't know well, and you're not sure what failure modes or invalidation strategies you should even be considering. What's a useful pattern here?",
    a: [
      "Have Claude ask you questions first, to surface considerations you may not have anticipated",
      "Write the implementation first, then ask Claude to review it for problems afterward",
      "Skip straight to writing tests, since tests will reveal any design gaps automatically",
      "Avoid giving Claude the task at all until you've researched the domain yourself",
    ],
    c: 0,
    e: "The interview pattern has Claude ask clarifying questions before implementing, which is specifically useful for surfacing design considerations like cache invalidation strategies or failure modes in an unfamiliar domain before code gets written. Reviewing after the fact means any missed considerations are now baked into the implementation, and tests don't substitute for surfacing design questions that were never asked.",
  },
  {
    d: "d2",
    src: "AR2-39, AR2-41",
    q: "Your CI job already runs Claude Code with a JSON schema, and you are writing the script that consumes what comes back. Which two things are true of that response? Select 2.",
    a: [
      "The structured result arrives in a structured_output field, beside metadata about the request",
      "The payload carries a cost estimate for the call, which is calculated client-side and can differ from the actual bill",
      "The response is the structured object itself, so the script can parse it without unwrapping anything",
      "A schema field declared with a format keyword is validated, so the script can trust the values it names",
      "The schema is echoed back in the payload, so a script can confirm which version the run used",
      "Session and usage metadata are written to a sidecar file rather than included in the response",
    ],
    c: [0, 1],
    e: "The response is a wrapper, not the object you asked for: request metadata such as the session id and usage sits alongside the structured result, which is carried in its own structured_output field, so a consuming script has to reach into it. The cost figures are worth knowing about for a different reason — they let a script track spend per invocation without going to the usage dashboard, but they are client-side estimates and are not the authority on what you are charged. The format keyword is accepted but treated as an annotation rather than enforced, so a value it describes still has to be checked.",
  },
  {
    d: "d2",
    src: "AR2-40",
    q: "A templating change leaves the JSON Schema your review step hands to Claude Code malformed, and nobody catches it before the pipeline runs. What does the run do?",
    a: [
      "It completes, but the run falls back to plain text output, so the script downstream gets prose where it expected objects",
      "It completes with exit code 0 and reports the schema problem inside the response payload, so the pipeline goes green on a run that produced nothing",
      "It stops there: claude exits with an error saying --json-schema is not a valid JSON Schema, followed by the validator's diagnostic",
      "It repairs the schema to the nearest valid shape and warns on stderr, so the findings come back against a schema nobody wrote",
    ],
    c: 2,
    e: "The schema is checked before any work happens, and a bad one is a hard stop with the validator's own diagnostic attached, so the job fails at the line that broke rather than three steps downstream. Nothing here degrades gracefully: there is no quiet fallback to prose, no repair pass inventing a contract, and no green run hiding the problem in a field the script may not read. That is the behaviour you want in a pipeline — a malformed contract should never produce a review that looks publishable.",
  },

  // D3 additions
  {
    d: "d3",
    src: "AR3-95, AR3-96",
    q: "Your automated review runs in two stages: a finding pass reads the diff, then a separate verification stage checks each finding against the code, ranks them and decides what gets posted. The finding pass is told to surface only issues it is confident are real. Real bugs are now being dropped before verification ever sees them. How should the finding pass be prompted instead?",
    a: [
      "To report every issue it notices, with a confidence and a severity attached to each finding",
      "To keep filtering at that stage, but against a numeric confidence threshold instead of a word",
      "To post the issues it is unsure about as low-priority comments that skip the verification stage",
      "To find and confirm in a single pass, so nothing can be lost in the handoff between two stages",
    ],
    c: 0,
    e: "Whatever the first stage declines to emit is gone: no later stage can rank, check or recover a finding it never receives, so a filter applied at the moment of discovery is the one place in this pipeline where information is lost for good. The pipeline already has a stage whose job is deciding what is worth posting. The first stage's job is coverage, and the confidence and severity it attaches travel with each finding as input to that decision rather than as a gate it applies to itself. Routing uncertain findings around the verification stage, or folding the two stages together, both leave the ranking step with less to work from.",
  },
  {
    d: "d3",
    q: "You want the model to pick the right tool when a user request is ambiguous, and to handle novel ambiguous cases sensibly, not just the ones you thought of. What's the most effective way to prompt for this?",
    a: [
      "A handful (2-4) of few-shot examples showing an ambiguous request, the tool chosen, and the reasoning for picking it over the plausible alternatives",
      "One exhaustive rule listing every ambiguous request you can enumerate together with the exact tool for each, so nothing is left to judgment",
      "A single generic instruction to choose the most appropriate tool, with the tool descriptions carrying the rest",
      "Dozens of few-shot examples covering as many specific requests as you can think of, so coverage is as wide as possible",
    ],
    c: 0,
    e: "A small set of targeted few-shot examples (2-4) that show the reasoning behind a choice teaches the model to generalize that judgment to new, unseen ambiguous cases, rather than just pattern-matching the exact examples given. An exhaustive rule list can't anticipate every case, a generic instruction gives no concrete guidance, and piling on dozens of examples isn't the point — a few well-chosen ones that show reasoning are what generalizes.",
  },
  {
    d: "d3",
    src: "AR3-49, AR3-50",
    q: "Supplier invoices in your extraction pipeline write dates as 03/04/26, 4 March 2026 and 2026-03-04, and amounts with either a comma or a period as the decimal mark. Your tool's JSON schema declares invoice_date and amount as strings, and the accounting system downstream accepts only one form of each. Where do the normalization rules belong?",
    a: [
      "In the prompt, alongside the schema — a schema fixes each field's shape and type, not the form of the value in it",
      "In the schema, as a regex or format constraint on each field, so values in the wrong form fail validation",
      "In post-processing code that tries each known layout in turn and drops the record when none of them matches",
      "Nowhere — declaring the fields as date and number types is enough to make the model emit one canonical form",
    ],
    c: 0,
    e: "A schema is a contract about structure: which fields exist, what type each one is, which of them are required. Two invoices can satisfy it perfectly and still disagree about how a date is written, because nothing in the contract reaches inside the value. Format-style keywords are among the parts the extraction path does not enforce, so tightening the schema does not close the gap either. Guessing layouts after the fact fails on exactly the documents that were unusual to begin with, which are the ones most worth getting right.",
  },
  {
    d: "d3",
    q: "Your pipeline has an enrichment step that must only run after metadata has been extracted. You want to guarantee that on this particular call, the model calls your extract_metadata tool and nothing else. What's the right setup?",
    a: [
      "tool_choice: \"auto\", trusting the model to pick extract_metadata first",
      "tool_choice: \"any\", so it's forced to call a tool of some kind",
      "tool_choice: {\"type\": \"tool\", \"name\": \"extract_metadata\"}, forcing that specific tool",
      "No tool_choice, but mention 'call extract_metadata first' in the user message",
    ],
    c: 2,
    e: "Forcing a specific named tool via tool_choice guarantees that exact tool runs on this call, which is what you need to sequence extraction before enrichment. \"auto\" could still return plain text, \"any\" could pick a different available tool, and relying on wording in the user message gives no hard guarantee.",
  },
  {
    d: "d3",
    q: "After switching an invoice-extraction prompt to tool use with a strict JSON schema, malformed JSON disappears completely. But you still occasionally get responses where the line items don't add up to the stated total. What does this tell you?",
    a: [
      "The schema is under-specified: adding more required fields would force the model to fill in every figure and the totals would reconcile",
      "Tool use eliminates JSON syntax errors, but it doesn't prevent semantic errors like values that don't add up — you still need separate validation for that",
      "Tool use is not being applied correctly — falling back to parsing free-text JSON would at least let you validate the numbers before accepting them",
      "The model needs a lower temperature, since arithmetic errors come from sampling variance rather than from the schema",
    ],
    c: 1,
    e: "Strict JSON schemas via tool use guarantee schema-compliant, syntactically valid output, but they say nothing about whether the values inside are internally consistent — a line-item sum mismatch is a semantic error, not a syntax one. That means you still need a separate validation step to catch it, regardless of schema strictness or temperature.",
  },
  {
    d: "d3",
    q: "Developers keep dismissing certain categories of findings from your review tool, but you can't tell why without manually reading every dismissal. What structured-output addition would let you systematically analyze which code constructs trigger the findings developers dismiss?",
    a: [
      "A free-text \"notes\" field the model fills in with its overall impression",
      "A \"detected_pattern\" field on each finding that records which code construct triggered it",
      "A higher temperature setting so the model varies its wording on repeated findings",
      "A bare \"confidence\" score with no further structure",
    ],
    c: 1,
    e: "Adding a detected_pattern field to each structured finding records which specific code construct triggered it, so you can later aggregate dismissals by pattern and see which ones are generating noise. A generic notes field or a bare confidence score doesn't give you that systematic, filterable signal, and temperature has nothing to do with tracking dismissal patterns.",
  },
  {
    d: "d3",
    src: "AR3-41, AR3-48",
    q: "Your document classifier returns a document_type enum holding the twelve types you knew about at design time. New contract types have started arriving, the model assigns each of them to the nearest value on the list, and they route to the wrong queue with nothing in the output marking them as unrecognized. What schema change fixes this?",
    a: [
      "Add an \"other\" value to the enum plus a detail string the model fills in with the type it actually saw",
      "Replace the enum with a free-text string so the model can name any type it encounters",
      "Add an \"unclear\" value for the model to pick whenever it cannot decide which of the twelve applies",
      "Regenerate the enum each month from the document types seen in the previous month's intake",
    ],
    c: 0,
    e: "The closed list is doing what it was designed to do — guaranteeing the value comes from a vocabulary the routing code understands — and that is also why an unanticipated type vanishes: the model has no legal way to say none of these, so it picks the nearest neighbour. Keeping the list for the twelve known types while giving the thirteenth somewhere to land, in its own words, is what lets routing see it and hold it. A plain string surrenders the guarantee for all twelve. An ambiguity value reports that the model could not choose between the known types, which is a different situation. And regenerating the list can only catch types that have already gone through wrong.",
  },
  {
    d: "d3",
    src: "AR3-80, AR3-81, AR3-75",
    q: "You've written a new extraction prompt and are about to put 40,000 documents through the Message Batches API with it. What should you do before submitting the full run?",
    a: [
      "Run a few hundred documents through first and refine the prompt on those results",
      "Submit the full run and cancel it as soon as the first returned results look wrong",
      "Submit the full run — request validation errors arrive as each request finishes, so a bad prompt surfaces early",
      "Split the 40,000 across ten batches submitted at once, so a prompt fault spoils only part of the run",
    ],
    c: 0,
    e: "A batch gives you nothing to watch while it runs. Results arrive as a single file once processing ends rather than as a stream, and validation of each request's params is asynchronous — those errors are reported when the batch as a whole has finished. So there is no early signal to react to, and ten simultaneous batches are ten runs of the same untested prompt. A short pass over a sample is the last point at which the prompt can still be changed, and lifting the first-pass success rate is what keeps you from paying to extract the same 40,000 documents twice.",
  },
  {
    d: "d3",
    q: "A large batch job comes back with a mix of successes and failures — some documents failed because they exceeded the context limit. What's the right way to handle this?",
    a: [
      "Resubmit the whole batch unchanged, since the failures look transient and a second run costs only the discounted batch rate",
      "Use the custom_id on each failed response to identify just those documents, then resubmit them after chunking the oversized ones",
      "Discard the failed documents, since a batch response cannot tell you which request each failure belongs to",
      "Switch the whole job to the synchronous API, which has no context limit to exceed in the first place",
    ],
    c: 1,
    e: "Each request/response pair in a batch is correlated by its custom_id, so you can pick out exactly which documents failed and resubmit only those, with a fix such as chunking documents that blew past the context limit. Resubmitting the whole batch wastes cost on requests that already succeeded, and failures are identifiable individually, not lumped together.",
  },
  {
    d: "d3",
    src: "AR3-53, AR3-51",
    q: "Your extraction service validates each result against its schema and re-prompts on failure, attaching the specific validation errors each time. Most documents pass on the second attempt. A handful never validate at all, and the service keeps re-prompting those until the job's time budget is gone. What is the loop missing?",
    a: [
      "A cap on attempts, with the document returned as an explicit error once the cap is reached",
      "A cleared prompt on each round, so the accumulated error text stops growing with every attempt",
      "A larger model on the later attempts, retried until one of them returns a valid result",
      "A fallback that stores the last response unvalidated once two attempts have failed",
    ],
    c: 0,
    e: "An unbounded correction loop turns one document's failure into the whole job's: the handful that will never come back clean spend the budget belonging to the documents that would. The Agent SDK's own structured-output loop is the shape to copy — it re-prompts on a mismatch, but only up to a retry limit, and once that limit is reached it yields an error instead of another attempt. That error is what lets the rest of the run finish and gives the failures somewhere to be counted. Keeping the last unvalidated response instead just moves the same problem into whatever reads the output next.",
  },

  // D4 additions
  {
    d: "d4",
    src: "AR4-83",
    q: "You ask the agent to find every occurrence of TODO(codegen) in a repo where build/ is gitignored. Grep reports none inside build/, while a Glob over the same folder lists its files happily. Why do the two disagree?",
    a: [
      "Grep only reads tracked files, because it runs through git grep under the hood",
      "Glob doesn't respect .gitignore by default; Grep skips gitignored files, so they see different sets",
      "Grep treats generated bundles as binary and skips them, while Glob only looks at names",
      "Glob was given a recursive ** pattern and Grep was not, so Grep never descended into that folder",
    ],
    c: 1,
    e: "The counter-intuitive part is which of the two is the permissive one. Unless CLAUDE_CODE_GLOB_NO_IGNORE=false is set before Claude Code starts, the name pattern walks past .gitignore and reports whatever sits on disk, while the line-level sweep honours the ignore rules. A sweep is therefore exhaustive only over what the repo tracks, which bites hardest exactly where generated output lives.",
  },
  {
    d: "d4",
    q: "A refund tool rejects a request because the amount exceeds what the customer is eligible for. What should the structured error response include so the agent stops retrying?",
    a: [
      "isRetryable: true, so the agent can retry with a smaller amount without asking the coordinator",
      "retriable: false plus a customer-friendly explanation of the business rule that was violated",
      "A full stack trace of the internal validation function that rejected the request",
      "Nothing beyond a plain success response, since business rules aren't real errors",
    ],
    c: 1,
    e: "Retrying won't change a business rule violation, so the response should mark it retriable: false and give a customer-friendly explanation the agent can relay to the user. Marking it retryable, hiding it behind a success response, or dumping a stack trace all fail to give the agent something it can act on appropriately.",
  },
  {
    d: "d4",
    src: "AR4-85, AR4-90",
    q: "The agent reads a 12,000-line generated client file and the result arrives with a PARTIAL view notice. It then calls Edit on a function it saw in that output, and the edit is refused. What happened?",
    a: [
      "Edit can't modify generated files, so the change has to go through Write with the full content",
      "The old_string spanned more lines than a single Edit call accepts, so it needs splitting in two",
      "A file large enough to trigger that notice is read-only to the agent for the rest of the session",
      "A PARTIAL view read does not satisfy read-before-edit, so the file still counts as unread",
    ],
    c: 3,
    e: "Read pages a file that is too large and says so: the notice reports how much arrived and how to fetch the rest with offset and limit. The edit precondition is a complete read in the current conversation, so a paged read leaves Edit blocked until the region is read properly, and it is also weak evidence about anything that was never in the window.",
  },
  {
    d: "d4",
    src: "AR4-18",
    q: "Your search_orders tool takes a parameter named user. In production the model fills it with an email, a display name, or an internal id depending on how the customer phrased the request, and lookups fail on arguments that look perfectly reasonable. What is the most direct fix?",
    a: [
      "Rename the parameter user_id and pin its type in the schema, since the name is what the model reads",
      "Accept all three forms and resolve them to an id inside the tool, so the model can't get it wrong",
      "Return a validation error naming the accepted identifier formats and let the agent try again",
      "Add a retry that feeds the failed lookup back to the model so it can try another identifier",
    ],
    c: 0,
    e: "The parameter name is part of the interface the model reads. user invites whatever the conversation happened to contain; the -id suffix states what belongs there, and a strict data model rejects the rest at the boundary instead of deep inside the lookup. Absorbing all three shapes keeps the ambiguity and moves the guessing into your own code.",
  },
  {
    d: "d4",
    q: "One agent in your system now has 18 tools available, and tool selection has gotten noticeably less reliable. What's going on, and what fixes it?",
    a: [
      "Nothing is wrong with having 18 tools — the model sees every description at once, so broader coverage can only help and the real problem is elsewhere",
      "Decision complexity rises with the number of tools; scoping the agent down to roughly the handful its role actually needs (not 18) restores reliable selection",
      "The fix is more tools, not fewer: each edge case gets a dedicated tool so no description has to cover two jobs",
      "The fix is longer, more detailed descriptions on all 18 tools, so the model can tell them apart",
    ],
    c: 1,
    e: "Giving an agent access to too many tools — the extreme case being 18 instead of a scoped 4-5 — increases decision complexity and degrades how reliably it picks the right one. The fix is restricting each agent's tool set to what its role needs, not adding more tools or renaming them.",
  },
  {
    d: "d4",
    src: "AR4-51, AR4-52",
    q: "Your extraction endpoint pins tool_choice to one named extraction tool, so every response arrives as structured arguments. Product also wants a one-line rationale in the text ahead of it, and no amount of prompting produces that sentence. Why?",
    a: [
      "Text and a tool call can't appear in one assistant message, so the rationale needs a second request",
      "The rationale is going into the tool arguments instead, so it never reaches a text block",
      "Forcing a tool prefills the assistant turn, so no text block is emitted before the tool call",
      "Named forcing isn't supported on the model in use, so it silently falls back to any",
    ],
    c: 2,
    e: "Forcing settings prefill the assistant message, and a prefilled message has no room for a text block in front of the structured output, so the instruction is not being ignored, it has nowhere to land. auto is the setting that leaves room for both, and forced selection is not supported on every model and setting anyway.",
  },
  {
    d: "d4",
    src: "AR4-98",
    q: "An exploration pass over an unfamiliar repo issues a dozen Read and Grep calls, then a series of Edits to apply the change. You want to cut wall-clock time. Which half of that actually runs in parallel?",
    a: [
      "Neither — the runtime executes each request in the order it was emitted, one after another",
      "The reads and greps — read-only calls run concurrently, while Edit, Write and Bash are serialised",
      "Both, as long as the calls arrive in one assistant message with no dependency between them",
      "The edits, since writes are queued and flushed together while reads block on the filesystem",
    ],
    c: 1,
    e: "The split is by side effect rather than by tool: read-only calls, including MCP tools marked read-only, can be in flight together, while anything that changes state runs one at a time so two calls cannot collide on the same file. Batching the discovery phase therefore pays off and batching the write phase does not.",
  },
  {
    d: "d4",
    q: "Your team wants a shared GitHub MCP server everyone on the project uses, while you personally also run a separate experimental MCP server just for your own testing. How should these be scoped?",
    a: [
      "Both belong in ~/.claude.json, since putting a server in a project file would need every teammate to hold the same credentials",
      "The shared server belongs in the project's .mcp.json; your personal experimental server belongs in your user-level ~/.claude.json",
      "Both belong in the project's .mcp.json, so the team can see the experimental server and adopt it if it works out",
      "MCP servers are discovered at connection time, so scope makes no difference to which ones the agent sees",
    ],
    c: 1,
    e: "Project-level .mcp.json is for shared team tooling that everyone working on the project should get, while user-level ~/.claude.json is for personal or experimental servers that shouldn't be forced on teammates. Mixing them the other way either hides the shared tool from the team or exposes your experiment to everyone.",
  },
  {
    d: "d4",
    q: "You're adding a shared MCP server to .mcp.json that needs a GitHub token, and you don't want the secret committed to the repo. What should the config do?",
    a: [
      "Hardcode the token in .mcp.json and add that file to .gitignore, so the value stays on each machine and never reaches the repository",
      "Reference it through environment variable expansion, e.g. ${GITHUB_TOKEN}, so the value comes from the environment instead of being written into the file",
      "Move the token into the MCP tool own description, so the model can read it when it needs to authenticate and nothing is written into the config file",
      "Store the token as a comment next to the server entry, since JSON comments are stripped before the file is parsed",
    ],
    c: 1,
    e: "Environment variable expansion like ${GITHUB_TOKEN} lets .mcp.json reference a credential without the actual secret ever being committed to the repo. Hardcoding it, putting it in a description the model reads, or leaving it in a comment all end up committing or exposing the secret.",
  },
  {
    d: "d4",
    q: "You try to use Edit to change one specific line, but it fails because the text you're matching appears more than once in the file. What's the reliable fallback?",
    a: [
      "Keep re-running Edit with the same match text, since it picks a different occurrence each time",
      "Read the full file, construct the corrected content, and use Write to save the whole file back",
      "Delete the file and recreate it from scratch with Write, which sidesteps the matching problem",
      "Use Grep to find every occurrence, then Edit the one you want by line number",
    ],
    c: 1,
    e: "When Edit can't find a unique anchor for the text it's supposed to change, the reliable fallback is to Read the whole file, build the corrected version, and Write it back — rather than retrying the same non-unique match or destructively recreating the file. Grep searches content across files; it doesn't make an Edit match unique.",
  },

  // D5 additions
  {
    d: "d5",
    src: "AR5-10, AR5-11",
    q: "An order-lookup tool returns more than 40 fields, but only 5 of them matter for handling a return. What should happen to that tool result before it stays in the conversation?",
    a: [
      "Leave it exactly as returned, since tool output should never be altered",
      "Trim it down to the return-relevant fields before it accumulates in context",
      "Ask the model to memorize all 40+ fields in case they're needed later",
      "Store the raw result in a database and otherwise ignore it",
    ],
    c: 1,
    e: "Tool results pile up fast and burn tokens on data irrelevant to the task at hand — a 40+ field order lookup where only 5 fields matter is exactly that case. Trimming to the relevant fields before the result lodges in context keeps the window useful instead of bloated. Leaving it untouched or stockpiling it for later just adds noise without adding value.",
  },
  {
    d: "d5",
    src: "AR5-20",
    q: "A team wants to decide when to escalate a support case by scoring the customer's message sentiment and by asking the model to self-report how confident it is. Why is this a weak design?",
    a: [
      "Sentiment and self-reported confidence are unreliable proxies for how complex the case actually is",
      "Sentiment scoring adds a model call to every message, and the latency outweighs the signal",
      "The two signals correlate, so combining them adds no information over using either alone",
      "Self-reported confidence cannot be calibrated, so no threshold on it can be meaningful",
    ],
    c: 0,
    e: "A customer can sound calm about a genuinely hard case, or upset about a trivial one, and a model's own confidence score doesn't reliably track actual case complexity either. Both signals are proxies that miss what you actually need to measure. The other options aren't the stated reason and aren't accurate claims either.",
  },
  {
    d: "d5",
    src: "AR5-24",
    q: "A customer asks the agent to match a competitor's lower price. Policy only covers price adjustments when the company's own site drops its price later, and says nothing about competitor pricing. What should the agent do?",
    a: [
      "Escalate, since policy is silent on this specific situation",
      "Approve the match, since it's clearly in the spirit of the existing policy",
      "Decline outright, since no policy explicitly grants a competitor match",
      "Apply the own-site adjustment policy anyway, since it's the closest existing rule",
    ],
    c: 0,
    e: "Policy gaps, not just complex cases, are a valid escalation trigger — when written policy doesn't address the customer's specific request, that ambiguity should go to a human instead of being resolved by stretching or ignoring an unrelated rule. Approving, declining, or repurposing the own-site policy all substitute the agent's own judgment for a decision policy doesn't actually cover.",
  },
  {
    d: "d5",
    src: "AR5-21, AR5-25",
    q: "A customer lookup for 'John Smith' returns three different customer records. What's the right move?",
    a: [
      "Pick the record with the most recent order, since that's usually the right one",
      "Ask the customer for an additional identifier, such as an order number or postcode",
      "Merge the three records and answer using combined data",
      "Pick whichever record the lookup tool returned first",
    ],
    c: 1,
    e: "Multiple matches call for clarification, not a guess. Asking for one more identifying detail resolves the ambiguity correctly, while picking a record by recency, list position, or merging records are all heuristic shortcuts that can point the agent at the wrong customer.",
  },
  {
    d: "d5",
    src: "AR5-32, AR5-36",
    q: "A subagent's database query times out on one attempt, and a later, successful attempt returns zero matching rows. How should these two outcomes be reported to the coordinator?",
    a: [
      "Both should be reported the same way, since from the coordinator's side neither attempt produced usable rows",
      "As two different outcomes: the timeout as an access failure needing a retry decision, the zero rows as a valid empty result",
      "Both as errors, so the coordinator's retry logic stays uniform and no failure can be silently swallowed as success",
      "Neither needs surfacing — the subagent should retry locally until it has rows, and report only what it finally found",
    ],
    c: 1,
    e: "A timeout means the coordinator couldn't reach the data and needs to decide whether to retry; a clean query that legitimately found nothing is a successful result requiring no retry. Collapsing both into 'no data' throws away the distinction the coordinator needs to make a sensible recovery decision.",
  },
  {
    d: "d5",
    src: "AR5-44, AR1-52",
    q: "A planned two-phase codebase review is about to move on. Phase one's conclusions run to file-by-file notes from every agent, and the module boundaries and owners are a small part of that. Phase two will spawn a fresh set of agents to audit error handling inside those modules, and nothing has crashed — this is a scheduled handover. What should happen at the boundary?",
    a: [
      "Summarize phase one's conclusions and put that summary into the phase-two agents' initial prompt, so they begin already knowing the boundaries",
      "Spawn the phase-two agents right away so they can start scanning, and send phase one's summary through once it has been written up",
      "Let the phase-two agents rediscover the module boundaries themselves, since a summary written by the mappers would bias what the auditors look at",
      "Have phase one write its findings to a scratchpad file, and let the phase-two agents open it if they need to",
    ],
    c: 0,
    e: "Both halves of the guide's rule are about timing — summarize before spawning, and put the summary into the new agents' initial context — and a spawned agent's context starts fresh, so what it begins with is whatever its prompt carried. Spawning first and sending after is not impossible, and that is not why it fails: the auditors scan without the boundaries for as long as the summary is in transit, and they begin the work on the wrong footing rather than the right one. A scratchpad the phase-two agents open if they need to still leaves them beginning from nothing: what is available on request is not what a fresh agent starts with. Rediscovery spends phase two's window redoing phase one.",
  },
  {
    d: "d5",
    src: "AR5-45, AR5-46",
    q: "An hour into exploring a large monorepo with Claude Code, the session has read dozens of files that turned out to be irrelevant to the task, and the context window is nearly full. You still need what the session found earlier. What's the direct move?",
    a: [
      "Run /clear so the next question starts from an empty context window",
      "Use /compact to reduce context usage, since the window has filled with verbose discovery output",
      "Add the irrelevant directories to the project's ignore settings so the same reads cannot happen again",
      "Start a fresh session and re-paste the findings you still need out of the terminal scrollback",
    ],
    c: 1,
    e: "On a large repository the defaults pull in instructions and file reads that have nothing to do with the task, and that is what has consumed the window here. Compacting is aimed at exactly that: the session so far is replaced by a summary, the space comes back, and the earlier findings survive into the summary rather than being abandoned. Clearing discards them, an ignore rule only changes what future reads pull in, and copying findings back by hand out of the scrollback keeps whatever you happen to notice.",
  },
  {
    d: "d5",
    src: "AR5-43, AR5-48",
    q: "A multi-agent codebase exploration needs to be resumable if the process crashes partway through. What's the right way to design for that?",
    a: [
      "Each agent exports its state to a known location, and the coordinator loads a manifest of that state on resume",
      "Restart the whole exploration from the beginning after a crash, since a partial run cannot be trusted",
      "Keep all agent progress only in the coordinator's live conversation, with no external record",
      "Have each agent post its findings to a shared chat log for a human to re-paste later",
    ],
    c: 0,
    e: "Crash recovery works when each agent's state lives somewhere durable and the coordinator has a manifest to read on resume, so it can reload that state and inject it back into agent prompts. Starting over discards everything already found, and keeping state only in a live conversation doesn't survive a crash at all.",
  },
  {
    d: "d5",
    src: "AR5-53",
    q: "For three months your extraction pipeline has run without human review on every field that clears its confidence threshold. Downstream teams have now started reporting errors in cases the pipeline scored as high confidence. What ongoing measurement would have surfaced this sooner?",
    a: [
      "Stratified random sampling of the high-confidence extractions, to measure their error rate and surface new error patterns",
      "A second model re-extracting every high-confidence field, with any disagreement between the two routed to a human reviewer",
      "Re-running the original labeled validation set each month and watching whether the threshold's accuracy holds",
      "Reviewing every extraction that scored below the confidence threshold, since that is where the errors concentrate",
    ],
    c: 0,
    e: "Automating a pool is what moves the risk into it, because it is then the pool nobody looks at any more. Sampling it on a stratified schedule keeps a live figure on how often it is wrong, and is the only one of these that can show a failure mode the pipeline has never produced before. A fixed validation set can re-test the failures it already contains and nothing else; a second extraction pass doubles the cost of every document to catch the cases two models happen to disagree about; and the sub-threshold pile is already the pile under review.",
  },
  {
    d: "d5",
    src: "AR5-77, AR5-66",
    q: "Each research subagent fetches its sources with the web fetch tool and records the retrieved_at timestamp it gets back as that source's date, so the synthesis agent can tell a real contradiction from two figures gathered at different times. Why doesn't this work?",
    a: [
      "retrieved_at is the fetch time, not a publication or collection date, so every source ends up stamped with the same day",
      "The timestamp is attached to the source rather than to each claim, so a claim quoted from it still travels undated",
      "Dates only separate the two cases when both figures come from the same publisher, which independent research sources rarely are",
      "Publication dates belong in the report's methodology section, not in the structured output each subagent returns",
    ],
    c: 0,
    e: "A date earns its place in a structured output by telling a reader when the underlying data was gathered, which is what turns an apparent disagreement into a gap in time. The web fetch tool reports something else entirely — the moment this pipeline happened to request the page — so the comparison it was meant to support can only ever come out a tie, and two figures years apart still read as a straight contradiction.",
  },

  /* ===== Scenario-based set — the 6 official scenarios from the Anthropic Exam Guide.
     3 questions per scenario (marked "official") are the guide's own Section 9 sample
     questions, reproduced from the candidate prep guide itself — not confidential exam
     content. The rest were written fresh, grounded in the same guide's task statements. ===== */

  // s1 — Customer Support Resolution Agent
  {
    d: "d1",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 1",
    sc: "s1",
    q: "Production data shows that in 12% of cases, your agent skips get_customer entirely and calls lookup_order using only the customer's stated name, occasionally leading to misidentified accounts and incorrect refunds. What change would most effectively address this reliability issue?",
    a: [
      "Add a programmatic prerequisite that blocks lookup_order and process_refund calls until get_customer has returned a verified customer ID.",
      "Enhance the system prompt to state that customer verification via get_customer is mandatory before any order operations.",
      "Add few-shot examples showing the agent always calling get_customer first, even when customers volunteer order details.",
      "Implement a routing classifier that analyzes each request and enables only the subset of tools appropriate for that request type.",
    ],
    c: 0,
    e: "When a specific tool sequence is required for critical business logic (like verifying customer identity before processing refunds), programmatic enforcement provides deterministic guarantees that prompt-based approaches cannot. The system-prompt and few-shot options rely on probabilistic LLM compliance, which is insufficient when errors have financial consequences. The routing-classifier option addresses tool availability rather than tool ordering, which is not the actual problem.",
  },
  {
    d: "d4",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 2",
    sc: "s1",
    q: "Production logs show the agent frequently calls get_customer when users ask about orders (e.g., \"check my order #12345\"), instead of calling lookup_order. Both tools have minimal descriptions (\"Retrieves customer information\" / \"Retrieves order details\") and accept similar identifier formats. What's the most effective first step to improve tool selection reliability?",
    a: [
      "Add few-shot examples to the system prompt demonstrating correct tool selection patterns, with 5-8 examples showing order-related queries routing to lookup_order.",
      "Expand each tool's description to include input formats it handles, example queries, edge cases, and boundaries explaining when to use it versus similar tools.",
      "Implement a routing layer that parses user input before each turn and pre-selects the appropriate tool based on detected keywords and identifier patterns.",
      "Consolidate both tools into a single lookup_entity tool that accepts any identifier and internally determines which backend to query.",
    ],
    c: 1,
    e: "Tool descriptions are the primary mechanism LLMs use for tool selection. When descriptions are minimal, models lack the context to differentiate between similar tools — expanding them is a low-effort, high-leverage fix for the root cause. Few-shot examples add token overhead without fixing the underlying issue, a routing layer is over-engineered and bypasses the model's own understanding, and consolidating the tools is a valid architectural choice but more effort than a first step warrants.",
  },
  {
    d: "d5",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 3",
    sc: "s1",
    q: "Your agent achieves 55% first-contact resolution, well below the 80% target. Logs show it escalates straightforward cases (standard damage replacements with photo evidence) while attempting to autonomously handle complex situations requiring policy exceptions. What's the most effective way to improve escalation calibration?",
    a: [
      "Add explicit escalation criteria to your system prompt with few-shot examples demonstrating when to escalate versus resolve autonomously.",
      "Have the agent self-report a confidence score (1-10) before each response and automatically route requests to humans when confidence falls below a threshold.",
      "Deploy a separate classifier model trained on historical tickets to predict which requests need escalation before the main agent begins processing.",
      "Implement sentiment analysis to detect customer frustration levels and automatically escalate when negative sentiment exceeds a threshold.",
    ],
    c: 0,
    e: "Adding explicit escalation criteria with few-shot examples directly addresses the root cause: unclear decision boundaries. It's the proportionate first response before adding infrastructure. Self-reported confidence is poorly calibrated on exactly the hard cases where the agent is already wrongly confident, a separate classifier is over-engineered before prompt optimization has even been tried, and sentiment doesn't correlate with case complexity, which is the actual issue.",
  },
  {
    d: "d1",
    sc: "s1",
    src: "AR1-09, AR1-10",
    q: "To keep the support console readable, your loop inserts a short assistant status message between the agent's tool_use blocks for get_customer and lookup_order and the user message carrying both tool_result blocks. What's wrong with that message history?",
    a: [
      "The status message is harmless — the real fault is both tool_result blocks sitting in one user message rather than two",
      "Nothing may sit between a tool_use block and its tool_result; the result message has to come immediately next",
      "An assistant message is what breaks the pairing — the same status line sent as a user message would be fine",
      "The status line has to come first in the result message's content array, ahead of the tool_result blocks",
    ],
    c: 1,
    e: "This is a structural rule about the history, not a style preference, and it holds whatever role the extra message carries. Inside the result message the tool_result blocks come before any text, which is where a status line can legitimately sit — or it can stay in the console and out of the history altogether. Splitting the two results across separate user messages would be a different mistake: every tool_use block from one response gets its result in that same reply.",
  },
  {
    d: "d4",
    sc: "s1",
    q: "lookup_order occasionally fails. A generic {\"error\": \"Operation failed\"} response makes it hard for the agent to decide whether to retry, ask the customer for more information, or escalate. What should the tool return instead?",
    a: [
      "Nothing more — the agent should infer the right action from the conversation, since it already knows what the customer asked for and what it tried",
      "A stack trace and the failing request payload, so the on-call engineer can reproduce the failure from the log alone",
      "Retry the call three times inside the tool before returning anything, so the agent only ever sees calls that truly failed",
      "A structured error response with an errorCategory (e.g., transient/validation/permission), an isRetryable flag, and a human-readable description",
    ],
    c: 3,
    e: "Uniform error responses like a generic 'Operation failed' prevent the agent from making an appropriate recovery decision. Structured metadata — an errorCategory, an isRetryable flag, and a human-readable description — lets it choose between retrying, escalating, or asking the customer for more information.",
  },
  {
    d: "d5",
    sc: "s1",
    src: "AR5-22",
    q: "A customer explicitly says 'I want to talk to a human, not a bot' before you've even looked into their billing dispute. What should the agent do?",
    a: [
      "Call escalate_to_human immediately, honoring the explicit request without first attempting investigation",
      "Attempt to resolve the issue first since it looks straightforward, then escalate if the customer insists again",
      "Ask the customer three clarifying questions before deciding",
      "Ignore the request and continue troubleshooting since escalation should be reserved for complex cases",
    ],
    c: 0,
    e: "Escalation triggers include explicit customer requests for a human, not just complex cases. Those requests should be honored immediately, without first attempting an investigation the customer didn't ask for.",
  },

  // s2 — Code Generation with Claude Code
  {
    d: "d2",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 4",
    sc: "s2",
    q: "You want to create a custom /review slash command that runs your team's standard code review checklist. This command should be available to every developer when they clone or pull the repository. Where should you create this command file?",
    a: [
      "In the .claude/commands/ directory in the project repository",
      "In ~/.claude/commands/ in each developer's home directory",
      "In the CLAUDE.md file at the project root",
      "In a .claude/config.json file with a commands array",
    ],
    c: 0,
    e: "Project-scoped custom slash commands belong in .claude/commands/ within the repository — version-controlled and automatically available to everyone who clones or pulls it. Of the alternatives, one puts the file under the developer's home directory, which is outside the repository entirely; one puts it in the file meant for project instructions and context rather than command definitions; and one names a configuration mechanism that does not exist in Claude Code.",
  },
  {
    d: "d2",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 5",
    sc: "s2",
    q: "You've been assigned to restructure the team's monolithic application into microservices. This will involve changes across dozens of files and requires decisions about service boundaries and module dependencies. Which approach should you take?",
    a: [
      "Enter plan mode to explore the codebase, understand dependencies, and design an implementation approach before making changes.",
      "Start with direct execution and make changes incrementally, letting the implementation reveal the natural service boundaries.",
      "Use direct execution with comprehensive upfront instructions detailing exactly how each service should be structured.",
      "Begin in direct execution mode and only switch to plan mode if you encounter unexpected complexity during implementation.",
    ],
    c: 0,
    e: "Plan mode is designed for complex tasks involving large-scale changes, multiple valid approaches, and architectural decisions — exactly what monolith-to-microservices restructuring requires, letting you explore and design before committing to changes. Direct-execution options risk costly rework when dependencies surface late, assume you already know the right structure, or ignore that the complexity is already known upfront rather than something that might emerge later.",
  },
  {
    d: "d2",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 6",
    sc: "s2",
    q: "Your codebase has distinct areas with different coding conventions: React components use functional style with hooks, API handlers use async/await with specific error handling, and database models follow a repository pattern. Test files are spread throughout the codebase alongside the code they test (e.g., Button.test.tsx next to Button.tsx), and you want all tests to follow the same conventions regardless of location. What's the most maintainable way to ensure Claude automatically applies the correct conventions when generating code?",
    a: [
      "Create rule files in .claude/rules/ with YAML frontmatter specifying glob patterns to conditionally apply conventions based on file paths",
      "Consolidate all conventions in the root CLAUDE.md file under headers for each area, relying on Claude to infer which section applies",
      "Create skills in .claude/skills/ for each code type that include the relevant conventions in their SKILL.md files",
      "Place a separate CLAUDE.md file in each subdirectory containing that area's specific conventions",
    ],
    c: 0,
    e: ".claude/rules/ with glob patterns (e.g., **/*.test.tsx) applies conventions automatically based on file path regardless of directory location — essential for test files spread throughout the codebase. Consolidating everything in root CLAUDE.md relies on inference rather than explicit matching, skills require manual invocation rather than automatic application, and subdirectory CLAUDE.md files are directory-bound and can't easily handle files spread across many directories.",
  },
  {
    d: "d2",
    sc: "s2",
    src: "AR2-19",
    q: "Your team's release-checklist command needs a version number to work, and teammates keep invoking it with no argument and getting a confused answer. Which frontmatter field is meant for this?",
    a: [
      "allowed-tools, which lists the tools the command may use without asking permission",
      "argument-hint in the frontmatter, which shows the expected arguments during autocomplete",
      "description, extended to spell out the expected argument in prose the model reads",
      "context, set to fork so the command runs in its own subagent context away from the chat",
    ],
    c: 1,
    e: "argument-hint exists for exactly this: it surfaces the expected arguments during autocomplete, so the shape of the invocation is visible at the moment someone types the command rather than discovered afterwards from a poor answer. A description tells the model what the command is for and is never surfaced as a prompt for parameters; one of the other fields concerns tool availability rather than input; and context: fork changes where the command runs, not what it is called with.",
  },
  {
    d: "d2",
    sc: "s2",
    src: "AR2-30, AR2-31",
    q: "A developer asks Claude Code to add one validation check to a single function, with a clear stack trace pointing at the bug. Should they use plan mode first?",
    a: [
      "Yes, because the change touches validation, and input handling always warrants a design pass",
      "Yes, because the file is shared with other features and a plan is the only way to see what else reads it",
      "No — this is a simple, well-scoped change, so direct execution is appropriate",
      "No, because plan mode blocks edits, and a fix this small would need a second approval round to land at all",
    ],
    c: 2,
    e: "The scope of the change is what decides this, and here it is one function with the fault already located. Direct execution fits, and reaching for a research phase you do not need only costs a round trip. The first two options both argue from something other than scope — the subject matter of the code, or the file's other readers — neither of which makes a located one-line fix into a design question. The last one has the mechanism right but the conclusion wrong: edits really are blocked until the plan is approved, and approving it is a single step rather than a reason to avoid planning when planning is warranted.",
  },
  {
    d: "d5",
    sc: "s2",
    src: "AR5-41, AR5-40",
    q: "Your team uses Claude Code across a multi-day refactor of a large, unfamiliar module. In a long session, it starts giving inconsistent answers and referencing 'typical patterns' instead of the specific classes it found earlier. What helps most?",
    a: [
      "Have Claude maintain a scratchpad file recording key findings, and reference it for subsequent questions",
      "Restart the session from scratch on a fixed schedule, so context degradation never has time to set in",
      "Lower the sampling temperature so the model stops drifting toward generic descriptions",
      "Ask Claude to re-summarize the whole conversation from memory before each new question",
    ],
    c: 0,
    e: "In extended sessions models can start giving inconsistent answers and referencing 'typical patterns' instead of the specific classes they found earlier. A scratchpad file that records key findings and gets referenced on later questions counteracts that drift.",
  },

  // s3 — Multi-Agent Research System
  {
    d: "d1",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 7",
    sc: "s3",
    q: "After running the system on the topic \"impact of AI on creative industries,\" you observe that each subagent completes successfully: the web search agent finds relevant articles, the document analysis agent summarizes papers correctly, and the synthesis agent produces coherent output. However, the final reports cover only visual arts, completely missing music, writing, and film production. When you examine the coordinator's logs, you see it decomposed the topic into three subtasks: \"AI in digital art creation,\" \"AI in graphic design,\" and \"AI in photography.\" What is the most likely root cause?",
    a: [
      "The synthesis agent lacks instructions for identifying coverage gaps in the findings it receives from other agents.",
      "The coordinator agent's task decomposition is too narrow, resulting in subagent assignments that don't cover all relevant domains of the topic.",
      "The web search agent's queries are not comprehensive enough and need to be expanded to cover more creative industry sectors.",
      "The document analysis agent is filtering out sources related to non-visual creative industries due to overly restrictive relevance criteria.",
    ],
    c: 1,
    e: "The coordinator's logs reveal the root cause directly: it decomposed \"creative industries\" into only visual-arts subtasks, completely omitting music, writing, and film. The subagents executed their assigned tasks correctly — the problem is what they were assigned. The other options incorrectly blame downstream agents that are working correctly within their assigned scope.",
  },
  {
    d: "d5",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 8",
    sc: "s3",
    q: "The web search subagent times out while researching a complex topic. You need to design how this failure information flows back to the coordinator agent. Which error propagation approach best enables intelligent recovery?",
    a: [
      "Return structured error context to the coordinator including the failure type, the attempted query, any partial results, and potential alternative approaches.",
      "Implement automatic retry logic with exponential backoff within the subagent, returning a generic \"search unavailable\" status only after all retries are exhausted.",
      "Catch the timeout within the subagent and return an empty result set marked as successful.",
      "Propagate the timeout exception directly to a top-level handler that terminates the entire research workflow.",
    ],
    c: 0,
    e: "Structured error context gives the coordinator what it needs to make an intelligent recovery decision — retry with a modified query, try an alternative approach, or proceed with partial results. A generic status after retries hides valuable context, marking a timeout as a successful empty result prevents any recovery, and terminating the whole workflow is unnecessary when recovery strategies could still succeed.",
  },
  {
    d: "d4",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 9",
    sc: "s3",
    q: "During testing, you observe that the synthesis agent frequently needs to verify specific claims while combining findings. Currently, when verification is needed, the synthesis agent returns control to the coordinator, which invokes the web search agent, then re-invokes synthesis with results. This adds 2-3 round trips per task and increases latency by 40%. Your evaluation shows that 85% of these verifications are simple fact-checks (dates, names, statistics) while 15% require deeper investigation. What's the most effective approach to reduce overhead while maintaining system reliability?",
    a: [
      "Give the synthesis agent a scoped verify_fact tool for simple lookups, while complex verifications continue delegating to the web search agent through the coordinator.",
      "Have the synthesis agent accumulate all verification needs and return them as a batch to the coordinator at the end of its pass, which then sends them all to the web search agent at once.",
      "Give the synthesis agent access to all web search tools so it can handle any verification need directly without round-trips through the coordinator.",
      "Have the web search agent proactively cache extra context around each source during initial research, anticipating what the synthesis agent might need to verify.",
    ],
    c: 0,
    e: "A scoped verify_fact tool applies least privilege: it covers the 85% common case of simple fact verification directly, while preserving the existing coordination pattern for the harder 15%. Batching creates blocking dependencies since synthesis steps may depend on earlier verified facts, giving synthesis full web-search access over-provisions it and violates separation of concerns, and speculative caching can't reliably predict what will need verifying.",
  },
  {
    d: "d1",
    sc: "s3",
    q: "A user asks a narrow factual question that the web-search subagent alone could answer. Should the coordinator still route the query through the document-analysis and report-generation subagents by default?",
    a: [
      "Yes — always run the full pipeline for consistency",
      "Yes, but skip the synthesis subagent only",
      "No — narrow questions should bypass all subagents and be answered directly with no delegation",
      "No — the coordinator should analyze the query and invoke only the subagents the query actually requires",
    ],
    c: 3,
    e: "A coordinator should analyze what a query actually requires and only invoke the subagents needed for it. Always routing through the full pipeline wastes subagent calls instead of matching effort to the question.",
  },
  {
    d: "d4",
    sc: "s3",
    q: "The synthesis subagent occasionally calls the web-search tool mid-synthesis instead of asking the coordinator to re-delegate. What's the underlying design problem?",
    a: [
      "The synthesis subagent's context window is too small to hold the findings it was given",
      "The web-search subagent is too slow, so synthesis stops waiting and does the lookup itself",
      "The synthesis subagent has tool access outside its specialization, which tends to get misused",
      "The coordinator's system prompt is too long for it to notice the re-delegation request",
    ],
    c: 2,
    e: "Agents given tools outside their specialization tend to misuse them, such as a synthesis agent attempting its own web searches. The fix is scoping each subagent's tools to its role and routing anything else, like a new search, back through the coordinator.",
  },
  {
    d: "d5",
    sc: "s3",
    src: "AR5-63, AR5-67",
    q: "The document-analysis subagent finds a statistic, and by the time it reaches the final report the source has been lost during summarization. How should subagents pass findings to prevent this?",
    a: [
      "Output structured claim-source mappings (source URLs, document names, relevant excerpts) that downstream agents preserve through synthesis",
      "Summarize each finding into plain prose as early as possible, so synthesis receives a compact input and stays inside its context budget",
      "Have the coordinator re-derive each source at report time from the queries it issued, rather than carrying attribution through every step",
      "Have subagents return the key facts they found instead of the excerpts the facts came from, so the coordinator can rank findings before writing",
    ],
    c: 0,
    e: "Source attribution gets lost when findings are compressed during summarization without preserving which claim came from which source. Subagents need to output structured claim-source mappings that survive being merged all the way through to the final synthesized report.",
  },

  // s4 — Developer Productivity with Claude
  {
    d: "d4",
    sc: "s4",
    q: "An engineer asks the agent to find every place a specific function is called across an unfamiliar codebase. Which built-in tool is the right first move?",
    a: [
      "Glob, to find files matching a naming pattern",
      "Bash, to write a custom search script from scratch",
      "Write, to create a new file listing all callers manually",
      "Grep, to search file contents for the function name",
    ],
    c: 3,
    e: "Grep searches file contents for patterns like function names, error messages, or import statements, which is exactly what finding callers requires. Glob only matches file paths by name or extension, so it wouldn't find the call sites at all.",
  },
  {
    d: "d4",
    sc: "s4",
    q: "The team wants the agent to read and update Jira tickets while exploring a legacy system. Should you build a custom MCP server for this?",
    a: [
      "Yes — a custom server keeps the tool surface under the team's own control and can be shaped to the exact ticket fields they use",
      "No — prefer an existing community MCP server for a standard integration like Jira, reserving custom servers for team-specific workflows",
      "Yes, because a community MCP server cannot be given the project-scoped credentials this workflow needs",
      "No — the Jira endpoints and field names belong in CLAUDE.md so the agent can call them directly without any server",
    ],
    c: 1,
    e: "For a standard integration like Jira, an existing community MCP server is the better choice over a custom build. Custom servers are worth reserving for genuinely team-specific workflows that no existing server covers.",
  },
  {
    d: "d2",
    sc: "s4",
    src: "AR2-36",
    q: "Your CLAUDE.md tells the agent to name the file and line number behind every snippet it quotes back. Custom subagents do it; the summaries that come back from the built-in Explore subagent never do. Why?",
    a: [
      "A subagent inherits CLAUDE.md only for the directories the parent session has already read files in, and Explore starts fresh at the repo root",
      "Explore and Plan skip CLAUDE.md files and the parent session's git status to keep research fast, while every other subagent loads both",
      "Explore runs with a read-only tool set, so an instruction that needs a tool it was not granted is dropped from its prompt",
      "A built-in subagent uses Anthropic's own system prompt, which takes precedence over project instructions that conflict with it",
    ],
    c: 1,
    e: "Two of the built-ins are deliberately cheap. Research is supposed to be fast and disposable, so those two start without the project's instruction files and without knowing what the parent session's working tree looks like; every other subagent, built-in or custom, gets both, which is exactly the difference the team is seeing. The practical consequence is that anything an exploration must obey belongs in the prompt that launches it, not in a file it never opens. Tool access is a separate axis and would not silently drop an instruction, and no vendor prompt is overriding the project here.",
  },
  {
    d: "d2",
    sc: "s4",
    src: "AR2-35",
    q: "While mapping an unfamiliar codebase's module structure, the discovery process generates a lot of verbose intermediate output the engineer never needs to see directly. What keeps this from flooding the main conversation's context?",
    a: [
      "Turning off tool-output logging during discovery so the transcript stays short",
      "Instructing the agent in CLAUDE.md to summarise every response it gives",
      "Adding disallowed-tools so discovery cannot call the tools with long output",
      "Using the Explore subagent to isolate the verbose discovery phase and return a summary",
    ],
    c: 3,
    e: "Explore exists for the discovery phase specifically: the listings, the excerpts and the dead ends all land somewhere the main conversation never sees, and what comes back is the finding rather than the trail that led to it. That is what keeps a multi-phase exploration from spending the whole context window before the actual work starts. The alternatives either suppress information that is needed, or reshape the codebase to work around a context problem.",
  },
  {
    d: "d1",
    sc: "s4",
    q: "You ask the agent to 'add comprehensive tests to this legacy codebase' — an open-ended task where the right subtasks aren't obvious upfront. What's the better decomposition strategy compared to a fixed sequential pipeline?",
    a: [
      "A fixed, pre-written checklist of the test files to create, followed in strict order so that no part of the legacy module gets skipped",
      "An adaptive plan: map the structure, identify high-impact areas, then create a prioritized plan that adapts as dependencies are discovered",
      "One single prompt asking for all the tests in one pass, so the model keeps the whole codebase in view",
      "Spawn one subagent per directory in parallel and merge whatever tests they each produce, so the work is spread evenly",
    ],
    c: 1,
    e: "Open-ended tasks like 'add comprehensive tests to a legacy codebase' don't have an obvious subtask list up front, so a fixed pipeline doesn't fit. An adaptive plan — map the structure, find high-impact areas, then prioritize — can adjust as dependencies are discovered along the way.",
  },
  {
    d: "d1",
    sc: "s4",
    q: "An engineer resumes a named session from yesterday to continue exploring a legacy module, but has since modified two of the files the agent already analyzed. What should they do before continuing?",
    a: [
      "Start from scratch and re-explore the whole module, since a stale analysis is worse than no analysis",
      "Resume the session and let the agent notice the changes itself when it next reads those files",
      "Resume the session and explicitly inform the agent about the specific file changes for targeted re-analysis",
      "Fork the session instead, so the stale analysis stays available on the original branch",
    ],
    c: 2,
    e: "Resuming a named session is only reliable if the agent knows what changed since it last looked at those files. Explicitly informing it about the specific file changes enables targeted re-analysis instead of relying on stale prior tool results or a full re-exploration.",
  },

  // s5 — Claude Code for Continuous Integration
  {
    d: "d2",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 10",
    sc: "s5",
    q: "Your pipeline script runs claude \"Analyze this pull request for security issues\" but the job hangs indefinitely. Logs indicate Claude Code is waiting for interactive input. What's the correct approach to run Claude Code in an automated pipeline?",
    a: [
      "Add the -p flag: claude -p \"Analyze this pull request for security issues\"",
      "Set the environment variable CLAUDE_HEADLESS=true before running the command",
      "Redirect stdin from /dev/null: claude \"Analyze this pull request for security issues\" < /dev/null",
      "Add the --batch flag: claude --batch \"Analyze this pull request for security issues\"",
    ],
    c: 0,
    e: "The -p (or --print) flag is the documented way to run Claude Code in non-interactive mode: it processes the prompt, outputs the result to stdout, and exits without waiting for input — exactly what CI/CD pipelines require. The other options reference non-existent features (a CLAUDE_HEADLESS variable, a --batch flag) or Unix workarounds that don't address Claude Code's actual command syntax.",
  },
  {
    d: "d3",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 11",
    sc: "s5",
    q: "Your team wants to reduce API costs for automated analysis. Currently, real-time Claude calls power two workflows: (1) a blocking pre-merge check that must complete before developers can merge, and (2) a technical debt report generated overnight for review the next morning. Your manager proposes switching both to the Message Batches API for its 50% cost savings. How should you evaluate this proposal?",
    a: [
      "Use batch processing for the technical debt reports only; keep real-time calls for pre-merge checks.",
      "Switch both workflows to batch processing with status polling to check for completion.",
      "Keep real-time calls for both workflows to avoid batch result ordering issues.",
      "Switch both to batch processing with a timeout fallback to real-time if batches take too long.",
    ],
    c: 0,
    e: "The Message Batches API offers roughly 50% cost savings but has processing times up to 24 hours with no guaranteed latency SLA — unsuitable for a blocking pre-merge check, but ideal for an overnight report. Relying on batches to be \"often fast enough\" isn't acceptable for a blocking workflow, batch results are correlated via custom_id rather than suffering ordering issues, and a timeout fallback adds complexity when simply matching each API to its use case is enough.",
  },
  {
    d: "d3",
    official: true,
    src: "Exam Guide v1.0 §9 Sample 12",
    sc: "s5",
    q: "A pull request modifies 14 files across the stock tracking module. Your single-pass review analyzing all files together produces inconsistent results: detailed feedback for some files but superficial comments for others, obvious bugs missed, and contradictory feedback — flagging a pattern as problematic in one file while approving identical code elsewhere in the same PR. How should you restructure the review?",
    a: [
      "Split into focused passes: analyze each file individually for local issues, then run a separate integration-focused pass examining cross-file data flow.",
      "Require developers to split large PRs into smaller submissions of 3-4 files before the automated review runs.",
      "Switch to a higher-tier model with a larger context window to give all 14 files adequate attention in one pass.",
      "Run three independent review passes on the full PR and only flag issues that appear in at least two of the three runs.",
    ],
    c: 0,
    e: "Splitting into per-file local passes plus a separate cross-file integration pass directly addresses the root cause: attention dilution across many files at once. Pushing the burden onto developers doesn't fix the reviewer itself, a larger context window doesn't solve an attention-quality problem, and requiring consensus across repeated passes would actually suppress detection of real bugs caught only intermittently.",
  },
  {
    d: "d2",
    sc: "s5",
    src: "AR2-38, AR2-39",
    q: "Your CI job already runs claude -p, so it no longer waits for input, but the review findings still arrive as prose the script has to scrape. The PR-comment step needs them as fields it can index into. What do you add to the invocation?",
    a: [
      "--output-format json together with --json-schema and a schema definition for the finding shape",
      "--output-format markdown, plus a prompt instruction to wrap the findings in a fenced JSON block the script can cut out",
      "--verbose, so the run prints each turn and the script reads the findings out of the transcript it emits",
      "Nothing on the CLI: -p already returns JSON, so the shape is settled by describing the fields in the prompt",
    ],
    c: 0,
    e: "How a run is invoked and what shape it returns are two separate decisions, and -p only settles the first. The documented way to pin the second is the two flags together: one says emit JSON, the other says which JSON, by pointing at a schema definition you wrote. Asking for the shape in the prompt instead works most of the time, and most of the time is the wrong reliability target for a step that posts to pull requests. A markdown format still hands the script prose to cut up, and --verbose changes how much of the run you can see rather than what it returns.",
  },
  {
    d: "d3",
    sc: "s5",
    q: "The same Claude Code session that generated a pull request's implementation is then asked to review its own diff before merge. Why is this less effective than a fresh review?",
    a: [
      "It retains reasoning context from generation, making it less likely to question its own prior decisions",
      "The generating session has already spent most of its context window on producing the diff itself",
      "It cannot see the final git diff from inside the session that produced it",
      "Code review needs a different model, since the one that writes code is tuned for generation",
    ],
    c: 0,
    e: "A model that generated code retains its own reasoning context from that generation, which makes it less likely to question decisions it already committed to. An independent review instance without that prior reasoning context is more effective at catching subtler issues.",
  },
  {
    d: "d3",
    sc: "s5",
    q: "Your automated review prompt tells Claude to 'be conservative and only flag high-confidence issues,' but false positives on comment-accuracy findings stay high and developers start ignoring the bot. What should you change?",
    a: [
      "Strengthen the instruction to be conservative, and add that it should only report findings it is highly confident about",
      "Lower the model's temperature so the review becomes more deterministic and stops flagging borderline comments",
      "Replace the vague instruction with explicit categorical criteria, e.g. flag a comment only when the claimed behavior contradicts the actual code behavior",
      "Disable the comment-accuracy category permanently, since a category developers have learned to ignore is no longer worth the review time it costs",
    ],
    c: 2,
    e: "General instructions like 'be conservative' or 'only report high-confidence findings' don't reliably improve precision on their own. Specific, explicit criteria for what counts as an issue are what reduce false positives and rebuild developer trust.",
  },

  // s6 — Structured Data Extraction
  {
    d: "d3",
    sc: "s6",
    q: "Some source documents never mention a 'discount_percentage' field your schema extracts. Some extractions come back with an invented value like 0% instead of leaving it blank. What schema design choice prevents this?",
    a: [
      "Make the field required so the model is forced to search the document harder before answering",
      "Remove the field from the schema entirely, since a field most documents lack is not worth extracting",
      "Add a longer instruction telling the model not to invent values it cannot find",
      "Make the field optional/nullable so the model can omit it when the source doesn't state it",
    ],
    c: 3,
    e: "A required field pushes the model to fabricate a value, like an invented 0%, just to satisfy the schema when the source never states it. Making the field optional/nullable lets the model correctly represent that the information simply wasn't there.",
  },
  {
    d: "d3",
    sc: "s6",
    q: "Incoming documents could be invoices, receipts, or contracts, and you have a separate extraction tool/schema for each type but don't know which applies until the model reads the document. How should you configure tool_choice?",
    a: [
      "Force a single named tool up front, e.g. {\"type\": \"tool\", \"name\": \"extract_invoice\"}",
      "tool_choice: \"auto\" so the model can reply with plain text instead if unsure",
      "tool_choice: \"any\" — the model must call a tool, but can pick which schema fits",
      "Disable tool use and ask the model to describe the document type in prose first",
    ],
    c: 2,
    e: "tool_choice: 'any' guarantees a tool call happens, so you always get structured output rather than a conversational reply. It still lets the model pick which of several extraction schemas actually fits the document, which matters when the document type isn't known in advance.",
  },
  {
    d: "d3",
    sc: "s6",
    q: "An extraction fails validation because a required 'invoice_date' is missing — and checking the source PDF shows the date was never printed on it at all. Will a retry-with-error-feedback loop fix this?",
    a: [
      "No — retries help with format or structural errors, but the date simply isn't in the source, so no retry can recover it",
      "Yes — including the specific validation error in the retry is what makes the model find the field it missed the first time",
      "Yes, as long as max_tokens is raised so the model has room to re-read the document properly",
      "Yes — the retry loop will find the date in the document metadata even when it is not printed on the page",
    ],
    c: 0,
    e: "Retry-with-error-feedback works when the model misformatted or misplaced something it could still find in the document. It's ineffective here because the date was never printed on the source at all, so no amount of retrying can recover information that isn't there.",
  },
  {
    d: "d5",
    sc: "s6",
    src: "AR5-55, AR5-52",
    q: "Your extraction pipeline reports 97% overall accuracy, so you're ready to remove human review entirely. What should you check first?",
    a: [
      "Nothing — at 97% the residual error is small enough that spot checks cost more than the errors they would catch",
      "The model's self-reported confidence per extraction, routing the low scores to review without calibrating them first",
      "Whether the validation set used to set the confidence thresholds is large enough",
      "Accuracy broken down by document type and field, since the aggregate figure can mask poor performance on specific segments",
    ],
    c: 3,
    e: "An aggregate accuracy figure like 97% can mask much worse performance on a specific document type or field hidden inside that average. Validating accuracy broken down by type and field is what actually confirms the pipeline is ready to run with less human review.",
  },
  {
    d: "d5",
    sc: "s6",
    src: "AR5-54",
    q: "You want to send only the extractions most likely to be wrong to your limited pool of human reviewers. What should drive that routing decision?",
    a: [
      "Route every extraction the model produced, regardless of confidence",
      "The length of the source document",
      "Field-level confidence scores calibrated against a labeled validation set",
      "A fixed random 10% sample of every batch, regardless of content",
    ],
    c: 2,
    e: "Field-level confidence scores, calibrated against a labeled validation set, tell you which extractions are actually likely to be wrong. Routing low-confidence or ambiguous extractions to reviewers targets limited reviewer capacity far better than a fixed random sample would.",
  },
  {
    d: "d5",
    sc: "s6",
    src: "AR5-69, AR5-65",
    q: "A scanned contract states a payment amount as '$4,500' in the body text but '$5,400' in an addendum table on the last page. Your extraction pipeline must handle this gracefully. What's the right approach?",
    a: [
      "Silently pick the value from the addendum table, since a later addendum normally supersedes the body",
      "Extract both values with a conflict_detected flag so the discrepancy is surfaced rather than resolved silently",
      "Average the two numbers, so the extracted figure sits between the two candidates",
      "Fail the extraction and route the whole document to human review before any field is used",
    ],
    c: 1,
    e: "When a source document genuinely contains conflicting values, silently picking one hides the discrepancy from anyone downstream. Extracting both values with a conflict_detected flag surfaces the conflict explicitly so a human or downstream rule can resolve it with full information.",
  },
];

/* ---------- Exam descriptor ----------
   Everything cca-trainer.js needs to render this track: the exam facts from the
   official guide, and every piece of prose that names the exam or its numbers.
   The prose lives here rather than in cca-trainer.js so that adding a track
   can't leave an Architect string behind in the Associate view — there is no
   hardcoded exam text in the renderer to forget. */
const EXAM_ARCHITECT = {
  id: "architect",
  tab: "Architect",
  credential: "Claude Certified Architect – Foundations",
  code: "CCAR-F",
  items: 60, // items on the real exam; exam-sim session length
  minutes: 120,
  passPct: 72, // 720 of 100–1000
  hasScenarios: true,
  scenariosDrawn: 4, // the exam presents 4 of the 6 (Exam Guide §3, §5)
  domains: ARCHITECT_DOMAINS,
  scenarios: ARCHITECT_SCENARIOS,
  questions: ARCHITECT_Q,
  copy: {
    eyebrow:
      "Claude Certified Architect · Foundations — 1 of 4 exams in Anthropic's Claude Certification Program",
    lede: "Active recall beats passive reading. Practice scenario questions weighted across the five domains, see where you stand per domain, and build toward a pass.",
    masteryHelp: `
        <p>This is your progress dashboard — read-only, it just reflects how you're doing.</p>
        <p>Each column's <b>width</b> is that domain's weight on the real exam — Agentic counts most (27%), Context least (15%). The coloured <b>fill</b> is your accuracy: the % of the questions you've <i>tried</i> in that domain that you got right. It's not a completion bar — 2 of 2 correct shows as a full 100%, because it measures how well you've done so far, not how much is left.</p>
        <p>The <b>legend</b> below reads "<i>X% of N tried</i>" per domain — your accuracy and how many you've attempted. "Not tried yet" means you haven't touched that domain.</p>
        <p><b>Weighted readiness</b> rolls all five domains into one number, each counted by its exam weight — a rough estimate of how exam-ready you are. Domains you haven't practised yet count as 0%, so one domain alone can't get you near 100%; it climbs as you cover more ground AND answer correctly. Aim for <b>72%+</b> (the real pass mark).</p>`,
    startHelp: `
        <p>Pick how you want to practise, then press <b>Start</b>.</p>
        <p><b>Mode · Practice</b> reveals the correct answer and an explanation after every question, so you learn as you go.</p>
        <p><b>Mode · Exam sim</b> hides the answers until the end, uses 60 questions, and runs a 120-minute countdown — like the real test. You can move freely between items, flag items to come back to, and submit when you're ready; unanswered items score as incorrect, and the exam submits itself when the clock hits zero.</p>
        <p><b>Scenarios</b> — the real exam presents <b>four of its six scenarios, picked at random</b>, and an exam sim here does the same: it draws four and asks every question this bank holds for each of them, six apiece. The four change from run to run, and a scenario's questions always appear together. The guide doesn't say how many questions the real exam attaches to a scenario, so read the six as this trainer's shape rather than the exam's — 60 items across four scenarios suggests the real thing leans more heavily on them than this does. <b>Practice doesn't use the four-of-six draw at all:</b> a weighted round takes scenario questions as they come, and drilling a single domain serves every scenario question that domain has. Either way each one carries its scenario description above it.</p>
        <p><b>One deliberate difference:</b> you can pause an exam sim and the clock stops. The real proctored exam has no pause — once it starts, the clock runs. So a paused run isn't a clean rehearsal of exam-day timing, and the summary tells you how many times you paused so you can judge that for yourself.</p>
        <p><b>Focus</b> (practice only) — "Weighted mix" samples across all five domains by their exam weight, or pick a single domain to drill it on its own. Exam sim always uses the weighted mix.</p>
        <p>Progress saves automatically, and you can pause mid-session and resume later. The <b>trash icon</b> in the bottom-right corner clears your mastery stats (your paused session and theme stay).</p>`,
    disclaimer: `
      This track covers <b>Claude Certified Architect – Foundations</b> (exam code CCAR-F). The trainer covers 2 of the 4 exams in Anthropic's Claude Certification Program — Architect and Associate, switchable with the tabs above — and not the remaining two. The questions are practice questions written to test the concepts in the five domains — not real exam items, which are secret and proctored. The five domains, their weights (27/20/20/18/15), the 60-item/120-minute format and the 720/1000 pass mark are all confirmed against the official Anthropic Exam Guide (v1.0, July 2026). Pricing, rate limits and context sizes change — verify such numbers in the official documentation before the exam. Its guide describes both multiple-choice and multiple-response items. The Claude Code domain now has both; the other four domains here are still single-answer, so a draw weighted across all five is lighter on multiple-response than the real exam. The Associate track has them throughout. An exam sim presents four of the six scenarios picked at random, which is the structure the guide describes — but only 24 of its 60 items belong to a scenario at all, where the guide calls the exam scenario-based throughout. A side effect worth knowing: the guide's twelve sample questions sit three apiece in four of the six scenarios, so every sim carries at least six of them. Another deliberate difference from the real exam: an exam sim here <b>can</b> be paused and the clock stops. The real proctored exam cannot, so the summary reports how many times a run was paused — check that number before you trust a score as a dress rehearsal.`,
  },
};
